# ppt4ai 开发进度

> **最后更新**：2026-08-22 · **当前阶段**：阶段 0 — 工程骨架 + IME 技术预研（未开始）

<!--
这份文件的首要用途是「跨会话交接」——新会话恢复上下文的第一入口。
更新时机：每完成一个可验证的里程碑就更新，并随代码一起 commit。
不要每写一个文件就更新，那会变成噪音。
-->

## 一、当前状态

设计阶段已完成。架构设计总纲已确认并落盘：[`docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md`](docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md)，含 28 条已确认决策、5 节技术设计、13 个阶段的路线图。

**代码尚未开始**。仓库目前只有 LICENSE、README 与该设计文档。

**下一个可验证的目标**：阶段 0 —— `pnpm build` 全绿，且一个最小 demo 证明「canvas 自绘光标 + 隐藏 contenteditable + 中文 IME 候选框跟随」这条路走得通。

> ⚠️ 阶段 0 的 IME 预研是**硬性前置**，不是可选的探索。它是全项目技术风险最高的一块；若走不通，纯 Canvas 架构需要重新考虑 —— 那种事必须在写几万行代码之前发现。

## 二、阶段进度

| 阶段 | 目标 | 状态 | 验收标准 |
|---|---|---|---|
| — | 需求梳理 + 架构设计总纲 | ✅ 完成 | 28 条决策确认，spec 落盘 |
| **0** | 工程骨架 + **IME 技术预研** | ⬜ 未开始 | pnpm build 全绿；demo 证明 canvas 自绘光标 + 隐藏 contenteditable + 中文 IME 候选框跟随可行 |
| 1 | model + geometry + render | ⬜ 未开始 | 加载 JSON → canvas 正确渲染；SceneGraph 快照测试建立 |
| 2 | pptx 导入（至可渲染） | ⬜ 未开始 | 打开真实 pptx 并正确显示；三级母版继承生效 |
| 3 | engine 编辑交互 | ⬜ 未开始 | 选择/变换/吸附/历史/z序/组合，全部可在 node 里断言 |
| 4 | 文本（layout + ProseMirror + IME） | ⬜ 未开始 | 中英混排、autofit、项目符号、竖排；换行位置与 PowerPoint 一致 |
| 5 | editor UI 外壳 + i18n | ⬜ 未开始 | element-plus 面板体系；zh-CN / en-US 双语 |
| 6 | pptx 导出（含无损回写） | ⬜ 未开始 | 逐字节往返测试通过；PowerPoint 打开无修复提示 |
| 7 | 表格 + 图片处理 | ⬜ 未开始 | 合并单元格、真实 `tableStyles.xml`、裁剪/滤镜 |
| 8 | 图表（lyCharts fork） | ⬜ 未开始 | 8 种图表可编辑数据 |
| 9 | 母版/版式编辑器 | ⬜ 未开始 | 改母版所有引用页自动更新；换主题换版式生效 |
| 10 | 动画 | ⬜ 未开始 | 三类动画 + 触发时序 + 切页；动画能导出到 pptx |
| 11 | player 放映/演讲者视图 | ⬜ 未开始 | 独立引入不背编辑器体积；备注、画笔、计时 |
| 12 | 打磨 | ⬜ 未开始 | 性能达标、EMF/OLE 三级降级、SmartArt 矢量渲染 |

图例：✅ 完成 · 🚧 进行中 · ⬜ 未开始 · ⚠️ 受阻

## 三、本阶段任务清单

阶段 0 的实施计划尚未产出（下一步用 writing-plans 技能生成）。计划产出后，这一节替换为逐条可勾选的任务，每条带「怎么验证」。

## 四、决策台账

开工前的 **28 条决策**见 spec [§8 决策台账](docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md#8-决策台账)，含每条的理由与已知代价。**此处只记录实施期间新增或推翻的决策**，避免两处重复导致漂移。

| 日期 | 决策 | 理由 | 状态 |
|---|---|---|---|
| — | （暂无） | | |

## 五、已知问题与风险

完整风险表见 spec [§9](docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md#9-已知风险)。最高两条摘录：

| 风险 | 等级 | 缓解 |
|---|---|---|
| canvas 自绘光标 + 中文 IME | 🔴 最高 | 阶段 0 强制预研；走不通则纯 Canvas 架构需重新考虑 |
| 图表 pptx 覆盖面（决策 18 的已知代价） | 🔴 高 | lyCharts 需自追 percentStacked / 次数值轴 / `c:numFmt` / 完整 `dLbls` / 对数轴 / 组合图 等，预估 2-4 个月 |

**待办的外部任务**：去 lyCharts 仓库修 4 个 bug（单例串扰、grid 单调缩小、33 处样式静默失效、arcTo 圆角柱报错）—— 独立任务，不占本项目工期（决策 19）。

**许可证纪律**（详见 spec §10）：

- PPTist 是 **AGPL-3.0**，网络传染 —— 绝不读、绝不移植、绝不派生其 `src/`
- `emf-to-png` 声明 MIT 实则打包 GPL wasm —— **禁装**

## 六、下次开工从这里继续

1. 读 spec：`docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md`
2. 用 **writing-plans** 技能为阶段 0 产出实施计划
3. 阶段 0 先做 IME 预研，再铺工程骨架 —— 顺序不要颠倒，预研结论可能影响骨架
