# ppt4ai 开发进度

> **最后更新**：2026-09-03 · **当前阶段**：阶段 6 主题字体方案切片已完成（`fontScheme` 进模型，`+mj-lt` 可解析）

<!--
这份文件的首要用途是「跨会话交接」——新会话恢复上下文的第一入口。
更新时机：每完成一个可验证的里程碑就更新，并随代码一起 commit。
不要每写一个文件就更新，那会变成噪音。
-->

**阶段 6 不支持媒体导入上报切片已完成**：EMF/WMF 等非位图媒体此前被无声跳过，现在通过 `ImportPptxOptions.onIssue` 可选回调上报 `{ code: 'unsupported-media', slideId, partPath, message }`，沿用 render 层既有的 issue 结构约定；不传回调时行为与之前完全一致，公开签名向后兼容。`ImportIssue` / `ImportIssueCode` 已从包导出。

**本切片否决了「导入为占位元素」方案，理由经实测确认**：`writeback.ts:1140-1141` 在模型元素占用了被跳过图片的 `expectedId` 时会抛出原始错误。导入侧与导出侧目前都跳过同一个 `p:pic`，索引因此天然对齐，这正是含 EMF 的包能安全往返的原因；插入占位元素会打破该对齐，把原本可保存的文档变成导出即失败。`emf-roundtrip.test.ts` 新增一项测试固定这个否决理由，防止后续有人重新尝试占位方案而不知代价。

**同时更正了上一轮的错误结论**：此前把 EMF 记为「静默数据丢失、风险高」，那是读代码推断未实测。实际 pptx 文件层面**不丢** —— 未编辑时走 byte-roundtrip 原样返回源字节；编辑其他元素时范围写回把 `p:pic` 节点与媒体字节作为未建模内容原样保留。真实影响是「编辑器中不可见、不可选」，风险等级已从 🔴 高改为 🟡 中。

pptx-import 3 个 EMF 测试、pptx-export 3 个 EMF 往返测试，全仓 69 个测试文件共 666 项；递归类型检查、串行生产构建、12 包边界检查和 `git diff --check` 均通过。实现提交为 `e15523b`、`d373e4b`。完整 EMF/WMF 解码仍受许可证阻塞（`emf-to-png` 声明 MIT 实为 GPL wasm，禁装），宿主如何呈现 issue（toast/日志/面板）留待接入方决定，本切片只提供数据通道。

**注 —— 偶发测试失败已定位并修复（提交 `b2c8882`）**：2026-09-01 多次观察到全量测试偶发失败，此前两次未能捕获失败项名称。按当时留下的建议（`pnpm test > log 2>&1` 单独保存输出）抓到：`packages/editor/src/image-asset-controller.test.ts > exports the controller API from the editor entry point`，报 `Test timed out in 5000ms` —— 该用例在**测试体内**动态 `await import('./index')`，于是整个 editor barrel（含 Vue 组件）的 transform 必须挤进 5s 单测超时，负载高时就超。根因是「测试内动态 import 大模块 + 默认 5s 超时」，**而非此前猜测的并行抢占**（该猜测已证伪）。全仓共 3 处同样写法（另两处在 `table-editor-overlay.test.ts` 与 `TableEditorOverlay.test.ts`），已一并改为**静态 `import * as editorEntry from './index'`** —— 开销移到模块加载阶段，vitest 不用 `testTimeout` 约束那里，断言逐字不变。**没有选择调大 timeout**：那只是把开销藏起来而非移走，且每新增一处这类测试都要重复设置。连跑 3 次全量均 806 项通过、耗时 ~9s。

**阶段 6 playground 主题面板接入切片已完成**：`ThemePanel` 从「只有单测覆盖契约」变为在跑起来的应用里可用。种子文档新增 `mst_playground` / `lay_playground` / `thm_playground`（`colors: {}`，全槽位继承 Office 默认），`shape_demo` 填充与表格首行两格改为 `scheme` 色引用（`accent1` / `accent2`），因此改色在画布与缩略图上立即可见。`asset-host` 新增 `setThemeColor(themeId, slot, color)`，主题缺失返回稳定 `theme-missing` 状态、engine 校验失败转为 `theme-color-failed`，均不抛错；`presentation-host` 新增 `setThemeColor(slot, color)`，沿 master 链解析当前页主题后经既有 `forward()` 通道转发，因此自动纳入当前页 engine 历史，undo/redo 与既有编辑操作行为一致。`App.vue` 侧栏常驻面板，`active` 绑定主题可达性。改色后 scene graph 的 `resolvedFillColor` 实测由继承默认色变为 `FF0000`、重置后回到 Office 默认 `4472C4`，验证的是解析后的渲染色而非模型字段。playground 6 个测试文件共 70 项、全仓 67 个测试文件共 660 项；递归类型检查、串行生产构建、12 包边界检查、Element Plus 扫描和 `git diff --check` 均通过。设计提交为 `daa6c3d`，实现提交为 `ca8841f`、`6d51abb`、`f760924`、`87f192c`。**已知限制**：主题编辑只作用于当前页，PPTX 真实语义里主题由 master 共享应影响所有引用该 master 的页；跨页同步需先给 engine 增加「文档级命令不记入单页历史」的能力（当前 `presentation-host.ts` 的 `undo()` 优先消费单页 engine 栈，任何广播实现都会与之冲突），已作为独立切片延期，并由 `presentation-host.test.ts` 中一条断言把该限制固化为可见行为。字体/格式方案编辑、主题增删切换、master/layout 绑定编辑与 PowerPoint/LibreOffice 人工阅读器验证继续延期。

**阶段 6 主题编辑 UI 与 history 切片已完成**：主题从「只能靠代码构造文档编辑」变为编辑器内可用。`@ppt4ai/engine` 新增文档级 `setThemeColor` 命令，按 `['themes', themeId, 'colors', slot]` 单槽位写入，复用既有 patch 机制自动生成 inverse，因此一次 undo 只撤销一个槽位；校验先在 clone 上跑，主题缺失/槽位非法/颜色非法均抛稳定错误且文档不被部分修改，无变化的写入不入 undo 栈。三态语义在 history 往返中精确保持——已核实 `toPatchValue` 只把 `undefined` 视为缺失，`null` 保持 present，故「重置为默认」与「继承源主题」不会互相退化。`@ppt4ai/editor` 新增 headless `theme-panel`（12 槽位分组元数据、hex↔`Color` 转换）、`theme-editor-controller`（沿 `slide.masterId ?? layout.masterId → master.themeId` 链推断生效主题，构建显式/重置/继承三态展示模型，无可达主题时返回空槽位而非报错）与 `ThemePanel.vue`（12 槽位平铺、按明暗/强调/超链接分组渲染、每槽位一个重置按钮、`aria-label` 与禁用态），并补齐中英文 i18n。渲染层未改动：`scenegraph.ts` 已从 master 链解析主题，改色即时反映到引用 scheme 色的元素。engine 与 editor 聚焦 35 个测试文件共 308 项、全仓 66 个测试文件共 648 项；递归类型检查、串行生产构建、12 包边界检查、Element Plus 扫描和 `git diff --check` 均通过。设计与计划提交为 `5d2e466`、`1d13cfa`，实现提交为 `3913169`、`d53da75`、`791c572`、`0a73b09`、`04d3d71`、`56dd231`。字体/格式方案编辑、「恢复继承」动作（写 `undefined`）、主题增删切换与 master/layout 绑定编辑、以及 PowerPoint/LibreOffice 人工阅读器验证继续延期；本机未宣称人工打开通过。

**阶段 6 主题颜色重置为 Office 默认值切片已完成**：`@ppt4ai/model` 从单一出处导出 `DEFAULT_THEME_COLORS`，`Theme.colors` 的槽位值放宽为 `Color | null`，`null` 表示「显式重置为 Office 默认值」，区别于 `undefined` 的「未设置、继续继承」。scheme 颜色解析对 `null` 槽位回退到默认色并保留既有 transform 与递归/环检测，`validateDocument` 接受 `null` 而不放松真实颜色的校验。`@ppt4ai/pptx-export` 的主题范围写回器把 `null` 槽位实体化为默认 `srgbClr` 后再走既有比较与替换路径，因此仍只触碰该颜色节点，保留槽位属性、未知兄弟节点、`extLst`、命名空间前缀和主题外壳；standalone 序列化同样把 `null` 与缺省都写成 Office 默认值，并复用同一常量消除硬编码色值的漂移。重置后的主题可重新导入得到显式默认色，重复导出保持确定性，文档、输入 `Uint8Array` 与 adapter-owned 字节不被修改。exporter 8 个测试文件共 93 项、聚焦 model/import/export 12 个文件共 184 项、`packages` 63 个测试文件共 627 项（该次未覆盖 `apps`/`scripts`）；递归类型检查、串行生产构建、12 包边界检查、Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `1eae099`。主题编辑 UI/history 与 PowerPoint/LibreOffice 人工阅读器验证继续延期；本机未宣称人工打开通过。

**阶段 6 已有 PPTX 图片外观源包写回切片已完成**：`@ppt4ai/pptx-export` 新增独立的图片外观范围写回器，已有 `p:pic` 现在可按模型更新 `a:off` / `a:ext` bounds、`a:xfrm` rotation/flip、`a:srcRect` crop、preset mask，以及有序 `alphaModFix` / `grayscl` effects；可添加缺失属性和节点、清除 optional 外观，并在清除 mask 时写回等价 `rect` geometry。补丁只触碰受支持范围，保留图片外壳、未知属性/节点、命名空间前缀、原引号、relationship、媒体字节和无关 ZIP 数据；效果类型变化同样保留原节点的未知属性。外观编辑可与 asset replacement 同时生效，reuse 与 clone 页面分别写入正确 part，重复导出保持确定性，文档、输入 `Uint8Array` 和 adapter-owned bytes 不被修改，未替换资产不读取 adapter。exporter 8 个测试文件共 90 项、聚焦 export/import 3 个文件共 97 项、`packages` 63 个测试文件共 623 项（该次未覆盖 `apps`/`scripts`）；递归类型检查、串行生产构建、12 包边界检查、Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `2b317b7`。高级裁剪/mask/effect 编辑 UI 与 PowerPoint/LibreOffice 人工阅读器验证继续延期；本机未宣称人工打开通过。

**阶段 6 PPTX master/layout 默认值与 color-map 写回切片已完成**：`@ppt4ai/model` 与 `@ppt4ai/pptx-import` 为导入的 master/layout 保留规范化 `source.partPath`，`@ppt4ai/pptx-export` 以范围写回方式选择性更新现有 placeholder 的 bounds、rotation、preset、fill、stroke、text/body，并写回 master、layout 与 source-backed slide 的 color-map 属性；缺失映射按源前缀插入，保留未知 XML、属性、关系、ZIP 条目和无关二进制。共享来源路径按模型 ID 确定性计算并检测冲突，缺失或损坏绑定源使用稳定错误；reuse/clone 页面均写入当前输出路径，blank 页面不触碰 source map。聚焦 model/import/export 验证 126 项，文档、输入 ZIP 与 adapter-owned 字节保持不变。实现提交为 `fd6aba9`；显式主题颜色删除、master/layout UI/history 以及 PowerPoint/LibreOffice 人工阅读器验证继续延期，本机未宣称人工打开通过。

**阶段 6 自定义 PPTX 主题颜色源绑定与写回切片已完成**：`@ppt4ai/model` 为 `Theme` 增加 clone-safe 的 `source.partPath` 绑定与路径校验；`@ppt4ai/pptx-import` 从 master/theme 关系保存规范化源路径，并对共享主题路径复用同一模型主题；`@ppt4ai/pptx-export` 通过范围扫描器只替换已编辑的 `clrScheme` 颜色节点，保留主题外壳、属性、未知兄弟节点、ZIP 条目和顺序，缺失或损坏绑定源使用稳定错误；standalone 生成按确定性的 master/theme 选择序列化有效主题颜色，省略 slot 继续使用 Office 默认值。编辑后的主题可重新导入并保留 transforms，未改主题保持字节一致，文档和输入 ZIP 字节不被修改。聚焦 model/import/export 测试 126 项，全仓 62 个测试文件共 603 项；递归类型检查、串行生产构建、12 包边界检查、Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `0db5b01`、`4e2ff02`、`f99141f`、`a68f38d`、`49bad28`、`c5e93ee`。master/layout 默认值与 color-map 写回、主题颜色显式删除、主题编辑 UI、真实 PowerPoint/LibreOffice 阅读器验证继续延期；当前环境未安装可用阅读器，未宣称人工打开通过。

**阶段 6 shape/text 旋转写回切片已完成**：`@ppt4ai/model` 为 `ShapeElement`、`TextElement`、占位符 `ElementDefaults` 增加 OOXML `1/60000` 度整数 `rotation` 契约与路径化校验；`@ppt4ai/pptx-import` 读取 shape/text、layout/master 的 `a:xfrm/@rot`，非法值局部忽略；`@ppt4ai/pptx-export` 源包写回只更新 `xfrm` 开始标签的 `rot` 属性，支持替换、添加和删除并保留未知属性、子节点、属性顺序和 XML 外壳；无源 serializer 同样输出旋转并可语义往返。聚焦 model/import/export 测试 112 项、全仓 61 个测试文件共 589 项，边界检查、递归类型检查、生产构建、Element Plus 扫描和 `git diff --check` 均通过。shape/text 旋转渲染、通用编辑手势和 engine 命令仍按范围延期；PowerPoint/LibreOffice 人工打开验证仍受本机环境限制。

**阶段 6 源版式与媒体内容类型兼容切片已完成**：`@ppt4ai/pptx-export` 按导入器稳定的 layout ID 索引源 `slideLayout` 关系，空白页可选择指定自定义版式，已有或复制页面切换版式时只更新对应关系 `Target`，保留关系 ID、顺序和其他 XML；新增或替换图片时按媒体扩展名复用匹配的 `[Content_Types].xml` `Default`，扩展名默认冲突时生成逐文件 `Override`，并对重复媒体声明保持确定性。旧的无内容类型测试夹具和既有页面/依赖写回路径保持兼容，文档、源字节和 adapter 字节仍不被修改。聚焦导出包 6 个测试文件共 53 项，全仓 61 个测试文件共 572 项；12 包边界检查、递归类型检查、串行生产构建、Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `b0fa530`、`d7c40ac`。PowerPoint/LibreOffice 人工打开验证仍受本机环境限制；custom master/theme 编辑和源 shape/text 编辑后完整写回仍待后续切片。

**阶段 6 源 shape/text 区间写回切片已完成**：`@ppt4ai/pptx-export` 现在按源元素顺序比较文本语义，仅在 `TextBody` 内容变化时替换 `p:txBody` 区间，保留 `p:sp` 外壳、未知 XML 和相邻元素；shape/text bounds 变化只更新 `a:off` / `a:ext` 的数值属性，保留 `xfrm` 属性、前缀、空白和未知子节点。编辑后输出可由 `importPptx` 重新读取并校验文本与 bounds；原有表格、图片、页面结构、版式关系和媒体内容类型写回保持兼容。实现提交为 `1fb30cf`、`dafb02f`。PowerPoint/LibreOffice 人工打开验证仍受本机环境限制；custom master/theme 编辑仍待后续切片。

**阶段 6 精确无修改源包字节往返切片已完成**：`@ppt4ai/model` 增加确定性的 64 位 FNV-1a `fingerprintBytes` / `fingerprintDocument`，文档 source 元数据可选记录 package 与 model 指纹；`@ppt4ai/pptx-import` 在完整模型组装后保存两者，`@ppt4ai/pptx-export` 仅在两个指纹都匹配时直接复制原始 source `Uint8Array`，保留压缩方式、ZIP 顺序和包外尾部字节，并完全跳过 adapter 读取。任一源字节或模型字段变化都会回落现有结构/依赖/元素写回，旧文档无指纹时保持兼容。指纹和 canonical serialization 不修改输入，`structuredClone` 可保留元数据。聚焦验证 8 个测试文件共 105 项，全仓 61 个测试文件共 567 项；12 包边界检查、递归类型检查、串行生产构建、Element Plus 扫描和 `git diff --check` 均通过。设计/计划提交为 `161fa10`，模型提交为 `f3059d5`，importer 提交为 `186040a`，exporter 提交为 `0ebf105`。PowerPoint/LibreOffice 人工打开验证仍受本机未安装阅读器及 `officecli` 缺少 `System.Private.Xml` 运行时限制。

**阶段 6 源包依赖 part 克隆与生命周期切片已完成**：`@ppt4ai/pptx-export` 新增浏览器安全的依赖关系图处理，复制源页面时递归克隆非全局内部依赖及其 relationship sidecar，保留 layout/master/theme 等共享 part，按同目录数字后缀确定性分配路径，并只重写克隆关系中的内部 `Target`；外链、关系 ID、未知属性和原始 part 字节保持不变。删除页面时按保留页面与克隆页面的保护闭包清理孤儿 slide、sidecar、notes/media-like 依赖及对应 content-type override，共享依赖和无关二进制条目保留；缺失依赖、坏关系 XML 和路径冲突使用稳定 `PPTX export dependency ...` 错误。重复导出保持 byte-identical，文档、源 ZIP 字节和 adapter-owned bytes 不变。聚焦验证 8 个测试文件共 105 项，全仓 61 个测试文件共 563 项；12 包边界检查、递归类型检查、串行生产构建、Element Plus 扫描和 `git diff --check` 均通过。设计/计划提交为 `b5abef4`，图模块提交为 `da9adcf`，克隆接入提交为 `be557e8`，生命周期清理提交为 `1b2d4a7`。本机未安装 PowerPoint/LibreOffice；`officecli validate` 因缺少 `System.Private.Xml` 运行时程序集失败，因此人工阅读器打开验证仍未宣称通过。

**阶段 6 无源 PPTX 生成切片已完成**：`@ppt4ai/pptx-export` 新增独立的 `createPptx(document, options?)` 路径，在不依赖 source ZIP 的情况下确定性生成 OPC package；共享 theme/master/layout 和固定关系拓扑已覆盖 `shape`、`text`、`table`、`image` 四类顶层元素。shape/text 支持几何、填充、描边、placeholder、结构化文本 body、项目符号/编号、marks、显式换行和 XML 安全转义；table 复用 canonical `a:tbl` serializer；image 支持 adapter 单次读取、字节拷贝、MIME 校验、跨页媒体去重、slide-local relationship 和 content-type defaults。生成结果可由 `importPptx` 重新读取，重复生成保持 byte-identical，文档与 adapter 字节保持不变，缺失映射、资产、组元素和 XML 控制字符使用稳定 `PPTX generation ...` 错误前缀。focused 验证 7 个测试文件共 99 项，全仓 60 个测试文件共 557 项；12 包边界检查、递归类型检查、串行生产构建、Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `6f1dae9`、`073f7b1`、`b893850`、`95e005d`。PowerPoint/LibreOffice 人工打开验证、真实源包依赖 part 克隆与关系生命周期、custom master/layout、group/chart/animation 及高级效果仍明确待办。

**阶段 6 PPTX 页面结构写回切片已完成**：`@ppt4ai/model` 为导入页面增加 `Slide.source` 来源绑定，`@ppt4ai/pptx-import` 保留原始 slide part、relationship 和 presentation ID；`@ppt4ai/pptx-export` 现按当前 `slideOrder` 确定性写回页面重排、删除、空白页插入和源页面复制，复用原页面或生成新的 slide XML、关系 part、presentation relationship、`p:sldId` 与 content-type override，并继续支持源表格/图片写回。未拥有的 ZIP 条目、未知 XML、关系目标和二进制资源保持不变，重复导出保持确定性，源文档和输入字节保持 clone-safe。聚焦验证 6 个测试文件共 91 项，全仓 59 个测试文件共 549 项；12 包边界检查、递归类型检查、串行生产构建和 `git diff --check` 均通过，Element Plus 扫描无匹配。设计/计划提交为 `f6b9827`，实现提交为 `382bbd3`、`0aaa168`、`14f0ed0`。无源文档完整 package 生成、依赖 part 克隆和 PowerPoint 人工打开验证保留为后续工作。

**阶段 7 图片旋转、翻转与中心缩放切片已完成**：`@ppt4ai/engine` 增加图片专用 `setImageRotation` 与 `toggleImageFlip` 原子命令，只修改变换字段并支持 no-op、校验失败、undo/redo 和 clone-safe history；`@ppt4ai/editor` 增加旋转手柄、OOXML 角度计算、Shift 15° 吸附、旋转预览，以及读取 Alt/Shift 的单选、多选和 group bounds 中心缩放与居中吸附；`apps/playground` 接入四个 UnoCSS 原生图片变换按钮、宿主命令、本地化状态和受控事件链路。变换范围仍限定为单图片：shape/text/table 通用变换、group/多选旋转、已有 PPTX 图片 `a:xfrm` 源包写回、旋转内容实时预览和高级裁剪编辑明确延期。editor 源码 31 个测试文件 227 项、engine 1 个测试文件 59 项、Playground 4 个测试文件 34 项、全仓 58 个源码测试文件 519 项均通过；12 包边界检查、递归类型检查、生产构建、Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `fd9f946`、`14f3cbd`、`9283f99`、`cc5fb64`、`5f77362`、`28c3a72`。

**阶段 7 多页导航、缩略图列表与页面选择同步切片已完成**：`apps/playground` 新增 presentation host，在宿主层管理两个独立 `EditorEngine` 页面并共享资产 adapter；切页不产生 history，selection、编辑结果和 undo/redo 按页面隔离，缩略图由同一页面状态即时生成。`App.vue` 使用 UnoCSS 原生缩略图按钮、`aria-current` 和现有 `ThumbnailCanvas` 渲染可切换页面，主编辑器、资产库、选择和状态面板均同步当前页；保留单页 `createPlaygroundAssetHost()` 兼容入口并支持注入文档/adapter。新增多页规格与实施计划，editor/Playground locale 保持中英文 key parity。Playground 源码 5 个测试文件 41 项、全仓 59 个源码测试文件 526 项、12 包边界检查、递归类型检查、生产构建、运行时 Element Plus 扫描和 `git diff --check` 均通过。规格提交为 `371746b`，计划提交为 `b332dc1`，宿主提交为 `e95bfb5`、`92a014e`，UI 提交为 `3b7a2e0`。

**阶段 7 页面新增、删除、复制与顺序调整切片已完成**：`presentation-host` 增加新增空白页、复制当前页、删除当前页和页面上下移动 API；复制页深拷贝当前文档并生成新的 page/document ID，页面 Engine、selection、history 继续隔离，删除始终保留至少一个页面，排序只调整 `slideOrder`。`App.vue` 接入 UnoCSS 原生页面操作按钮与边界禁用态，状态区和中英文 locale 补齐页面结构操作反馈。Playground 源码 5 个测试文件 47 项、全仓 59 个源码测试文件 532 项、12 包边界检查、递归类型检查、生产构建、运行时 Element Plus 扫描和 `git diff --check` 均通过。实现提交为 `b738fad`。

## 一、当前状态

**阶段 7 group 往返切片已完成**：修掉了上条里程碑点名的最大保真缺口 —— 分组文档此前导入即丢失分组结构。按三步推进，每步独立验证。

**第一步：导入 `p:grpSp`**（提交 `fba0b03`）。此前 `findSlideElements` 只匹配 `sp`/`graphicFrame`/`pic` 并**递归穿过** `grpSp`，把组内子元素当顶层元素平铺，`GroupElement` 只能由编辑器的 group 命令产生。改为保留组结构，两条硬约束：组用 `grp_N` 前缀、叶子保持连续 `el_N`（写回扫描器按同样三个 tag 编号，序号一动就全线错位）；**组必须排在子元素之前**，否则 `documentToSceneGraph` 的 `visited` 去重会让叶子先以顶层节点渲染、永远收不到级联。`grpSpPr` 无可用 bounds 的组被丢弃、子元素降级为叶子。

**第二步：写回扫描器镜像树结构**（提交 `5c3bd72`）。这是预告过的风险点，失败也如期出现（`element prefix mismatch`）。**值得记的是字节级往返测试反而先通过** —— 未编辑时走 byte-roundtrip 直接返回原字节，根本不进扫描器，所以这个 bug 只在「编辑了分组幻灯片里任何元素」时才暴露，比看起来更隐蔽。改法是让扫描器与导入器对称：`grpSp` 占一个 `grp_N` 槽位、带 `group` 标记、无可写内容，两条写回路径都跳过它 —— 唯一例外是组自身旋转，必须落在 `grpSpPr` 而非某个子元素上，这条单独立测试钉住。

**第三步：`a:chOff`/`a:chExt` 子坐标空间**（提交 `da350ce`）。全仓此前**零处**读取这两个属性，导致 PowerPoint 里被缩放过的组，其子元素坐标全错。**这一步的第一版实现是错的，且比原 bug 更糟，值得完整记录**：最初在导入期就把子元素 bounds 重映射到 slide 空间，8 项测试全绿；但 `boundsReplacements`（`writeback.ts:203-205`）是拿模型 bounds 与**源 XML 原始值**比较，重映射后每个子元素在每次导出时都「看起来被编辑过」而被覆写成 slide 空间数值，同时 `chOff`/`chExt` 声明保持不动 —— 文件内部自相矛盾，PowerPoint 会把子元素再缩小一次。原 bug 只是只读的显示错误，这个却是**在与该组无关的编辑上往用户文件里写入损坏**。改为把 `chOff`/`chExt` 原样存进 `GroupElement.childSpace`、由 `documentToSceneGraph` 映射，与 group 旋转已定的决策一致：模型保持「作者原始值」这一唯一真相，写回永远比较同一套数字，渲染层负责合成。映射**内层优先、且在旋转之前**，因此旋转支点取的是组映射后的 slide 中心。那个捕获损坏的导出测试保留在 `scaled-group-writeback.test.ts`。

全仓 80 个测试文件 782 项通过（新增 group 导入 6 项、group 写回 7 项、子空间导入 6 项 + 渲染 8 项、写回回归 2 项）；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。

**追加修正（提交 `e3c4f75`）—— 上一段「仍未做」写错了一半**：原文称 `chOff`/`chExt`「只读不写」、缺口只在导出方向，**这个判断是错的，读取方向本身就是坏的**。`move` / `resize` / `resizeSelection` 三处都用 `descendantElementIds` 递归改写全部后代 bounds；一旦组带 `childSpace`，scene 层已经在做映射，engine 再改写一遍就是**变换施加两次**：移动 1000000 EMU 子元素实际位移 1500000，缩放组则子元素按目标倍率的平方缩放。写测试探这个缺口时才发现，不是靠读码。修法是新增 `boundsMappedElementIds`，**在任何带 `childSpace` 的组处停止下降**，把后代留在映射所预期的作者空间里；不带 `childSpace` 的组（编辑器自建的组）行为完全不变。全仓 81 个测试文件 788 项通过。

**追加二：group bounds 与 childSpace 写回（提交 `be12a9e`）**：此前 group 写回只处理 rotation，因此组的移动/缩放在导出时被整体丢弃。现在从模型写回 `off`/`ext` 与 `chOff`/`chExt` 两对。**实现上有个必须避开的陷阱**：这两对都在 `p:grpSpPr` 下同一个 `a:xfrm` 里，而既有的 `firstDescendant` 是深度优先搜索，在 `grpSp` 上调用会**穿透到组内某个子形状自己的 `a:xfrm`** 并改错元素；因此新增 `groupTransform` 只取直接子节点。`rotationReplacements` 原本有同样的潜在隐患（此前只因组内首个子元素恰好也带 `xfrm` 而未暴露），一并改为 scope 到 `grpSpPr`。两对各自先比较再替换，未改动的组不产生任何 replacement，字节级往返不受影响。全仓 82 个测试文件 794 项通过。

**追加三 —— 前两条里「`childSpace` 不随组 resize 同步」这个待办是伪缺口（提交 `2efc4e6`）**：我按该待办去实现「resize 时等比缩放 `childSpace`」，理由是保持 `ext:chExt` 比例不变；**但那个比例本身就是缩放系数** —— 固定它意味着组框变大而内容原地不动。改完立刻有 3 项既有测试变红，其中一项正是上一轮我自己写的「resize 后子元素仍充满整个组」。因此代码已回退，改为把真实契约钉成测试：**`bounds` 缩放、`childSpace` 保持导入值、内容随组框一起缩放**。

**同时撤回一个未经核实的断言**：前两轮里程碑我写过「PowerPoint 的行为是同步缩放二者」。**这一条我无法验证** —— 本机没有 PowerPoint/LibreOffice，且它是关于另一个应用行为的断言、不是关于本仓库代码的。当时就该标注不确定而不是写成事实。现取的 A 方案（内容随组缩放）是我的判断，不是核实结论；若将来在真实阅读器里发现 PowerPoint 确实保持内容尺寸，需连带修正那 3 项测试。全仓 82 个测试文件 799 项通过。

**追加四：ungroup 烘焙组变换（提交 `57a9e0f`）**：此前 ungroup 直接删掉组、子元素原样留下，因此带旋转或 `childSpace` 的组一解组，内容会**视觉跳变** —— 那两个变换本由 scene 拍平时施加，随组一起消失。现在把整棵子树烘焙成绝对值，复用 scene 层同样的 `cascadeRotation` 与 `mapChildSpace`，并清掉中间层 group 自己的 `rotation` / `childSpace`，避免变换被施加两次。

**一个更省事的做法被探测排除了**：我原本想把外层角度「推给」存活下来的嵌套 group（只改一个字段）。写了临时探测脚本对比两种策略后发现不行 —— **group 的旋转永远绕自身中心，而外层旋转绕的是外层组中心**，只要后代在父组里不居中，两个支点就不重合，实测偏差 500000 EMU。因此角度必须落到叶子上，嵌套 group 的 `rotation` 保持为空，这条已写成测试断言（用一个刻意偏心的叶子夹具）。

全仓 83 个测试文件 806 项通过；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。

**仍未做**：多选整体旋转、group 翻转继续延期。**人工阅读器验证仍未做**（本机无 PowerPoint/LibreOffice），以上全部结论来自单元测试；尤其「组缩放时内容随之缩放」是本项目自选的语义，未与真实 PowerPoint 对照过。

**追加五：多选整体旋转（提交 `c75a23e`、`099c9ae`）**：旋转此前只能逐元素，多选没有整体转动的途径。新增 `rotateSelection`：每个选中根各自加上角度，其中心绕联合中心公转，一次 history。**探针抓到了 group 情形的坑** —— 让选中 group 的后代保持不动看起来是对的（scene 拍平时本就会级联组角度），但级联的支点是组中心，组一公转支点就移位，后代必须随之平移，与 `move` 改写后代同理；它们保留各自角度，只有中心按组的位移量偏移。入口侧把旋转手柄从「单选才显示」放宽为「选区有 bounds 就显示」，手势的 `elementId` 变为可选（多选时缺省），因为它产出的是施加给每个成员的**增量**而非某个元素的绝对角度。**翻转了一条既有断言**（原「多选时隐藏旋转手柄」编码的是缺失功能而非期望行为）。

**追加六：shape/text/table/group 翻转（设计 `10849b8`，实现 `1883d26`…`b13b66d` 共 7 提交）**：这是旋转链路收尾后的最后一项延期。**动手前写探针确认缺口真实存在**：导入带 `flipH="1" flipV="1"` 的 `p:sp`，回来的 shape 完全没有翻转数据 —— 与 group/`chOff` 同一类保真缺口，源包信息在导入时被静默丢弃。四种 kind 各加裸 `flipH`/`flipV`（紧邻既有 `rotation`，不迁移到 `ElementTransform`）；`flipH="0"` 与属性缺失等价，模型里不存 `false`，写回侧因此对源包的 `flipH="0"` **不产生任何字节改动**（有测试专门固定，避免无意义 churn）。

`cascadeRotation` 升级为 `cascadeTransform` 而非并列新增一个翻转级联 —— 翻转与旋转在同一条祖先链上交替作用且不可交换，拆开就得在调用方重新交错，那正是最容易错的地方。**镜像的三点数学与旋转不同，都由测试直接固定**：绕组中心轴反射（等距变换，宽高精确不变）；后代翻转按位异或，组与后代翻同一轴时相互抵消（两次绕平行轴的镜像合成为恒等），字段应当消失而非叠加；**单轴镜像反转角度旋向**（`rotation` 取负），双轴是点反射不反转旋向，且双轴本身等价于半周旋转、由后代自己的两个翻转字段承载。每个祖先内部先镜像后旋转。

**翻转只镜像几何，不镜像文本与单元格**：新增 `withFlipAndRotation` 供 shape 使用，text 与 table **继续走 `withRotation`**，两处各有一条测试固定「翻转不产生 scale 调用」。依据是微软支持文档（Flip 翻转对象但文字需先转图片才能镜像），**本机无阅读器、未实测**，该出处已写入设计文档。ungroup 的烘焙同时扩展：翻转现在也是触发条件，否则解组会视觉跳变（与追加四同一类问题）。UI 侧两个翻转按钮从 image 单选改为跟随 `selectedRotatableNode`，复用旋转切片已建立的 computed；顺带补上 `element-rotated` 的两份 locale 条目 —— asset-host 此前已在发送该 message，但两份 locale 里都没有对应文案。

全仓 88 个测试文件 885 项通过；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。

**追加七：多选整体翻转（提交 `3032c31`、`21a292d`）—— 并否决了追加六自己写的实现思路**：追加六把多选翻转列为延期时写「需绕联合中心镜像并重算各元素 bounds」。**动手时否决了这句话** —— 那描述的是「镜像整个排布」，会把左边的元素搬到右边去，是伪装成翻转的布局改动。改用逐元素翻转、**不动任何 bounds**，依据是微软文档明确写的「多选旋转时各绕自身中心而非作为一组」，翻转同属 Arrange > Rotate 菜单、语义一致。**因此 `flipSelection` 与 `rotateSelection` 形状不同**（后者确实绕联合中心公转并移动 bounds），这个不对称是有意的，两处注释都写明。同时选中 group 与其后代时后代被跳过（复用 `selectionRoots`），否则两次翻转相互抵消、看起来像那个元素没反应。

**探针记录一处易误判为 bug 的区别（`flip-probe.test.ts`）**：对比「翻转组」与「翻转组内居中的子元素」，预期渲染相同 —— **未旋转时确实相同，子元素带旋转时不同**（`rotation` 一个取负一个不取）。查证确认**这不是 bug**：组绕屏幕对齐的轴镜像，会反转后代角度的旋向；翻转元素自身是在它已旋转的框内镜像，不反转。子元素一旦带角度，两条镜像轴就不再平行，结果本就该分岔。该探针留在仓库里把区别钉成断言，防止后来者把其中一条「修正」成另一条。

**顺带修掉旋转的一处遗漏**：旋转手柄早就支持多选（`rotationHandleVisible` 含 `rotatesAsSelection`），但**工具栏的左右旋转按钮仍只在单选时出现** —— 那是上一个旋转切片的遗漏而非设计。两种变换现在共用一个 `transformEnabled` 门禁：能拖的选区也能点。多选时旋转按钮发的是**增量**而非绝对角度，与手势路径一致。

全仓 90 个测试文件 901 项通过；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。**剩余延期只有一项**：文本镜像语义的阅读器实测（本机无 PowerPoint/LibreOffice）。

**阶段 6 文本格式导入切片已完成（设计 `33dd0d2`，实现 `5bb9e94`、`3a86c07`、`2c78838`、`a464f96`）**：这是探针挖出来的、比清单上任何一项都严重的保真缺口 —— `a:rPr`、`a:pPr`、`a:bodyPr` 在导入时被**完全丢弃**。实测导入一个带 `sz="3200" b="1" i="1" u="sng"` + 红色 `solidFill` + `<a:latin typeface="Georgia"/>` 的 run，回来是 `{"text":"Styled"}`；段落属性与 body 属性同样是 `undefined`（只有 `vert` 被读）。

**值得记的是其余四层早已就绪**：模型的 `TextMarks`/`TextParagraphAttrs`/`TextBodyProperties` 齐全且有校验；`layout.ts` 消费对齐、行距、段间距、insets、垂直对齐、`marginLeft`/`indent` 与三种 autofit；`text-painting.ts` 从 marks 拼出 canvas `font` 并画下划线；`standalone-xml.ts` 把三者全部写回。**只有导入端从不读** —— 所以这不是"实现一个新特性"，而是接上一条本该通的线。

**由此产生的真实故障（探针确认，非推断）**：不动文本时写回逐字节保留源 `rPr`；**改一个字符后重新序列化 body，输出成了裸 `<a:r><a:t>Edited</a:t></a:r>`，Georgia、字号、颜色全丢**。根因不在写回，而在模型从未捕获这些值。

**顺带修掉一个位置错误的 bug**：`a:br` 此前被"数个数、在末尾补等量 `\n`"，只有单 run 段落时凑巧正确 —— `Above<br/>Below` 导入成 `"AboveBelow\n"`。改为按顺序遍历段落子节点。**同时发现扁平 `text` 字段的段落拼接也是错的**：段落原本首尾相连，而 `normalizeTextElement` 是按 `\n` 切分重建段落的，所以四个段落回来会塌成一个。两处一并改为段落间插 `\n`，三条既有断言随之更正（它们编码的是计数行为）。

**三条明确的"不猜"决策**：① 只读 `lnSpc/spcPct` 与 `spcBef|spcAft/spcPts`，因为模型的 `lineSpacing` 是百分比而 `spaceBefore` 是 EMU —— 另一种配对**跳过而非换算**，猜单位会让文字错位；② 四个 inset 齐全才记录，模型没有部分形态；③ **不把 `spAutoFit@lnSpcReduction` 读成 `maxHeight`** —— 按 ECMA-376 该属性属于 `normAutofit`，`spAutoFit` 根本没有属性，**导出侧 `serializeAutofit` 那行是错的**，但改它会变动既有 standalone 字节，记入设计文档留作独立切片，读回来只会把错误固化。

**模型一处放宽**：`indent` 校验从非负改为带符号 —— OOXML 悬挂缩进本就是负值（`marL="457200" indent="-228600"` 是标准项目符号写法），另三个测量值保持非负。**一条断言意义变更**：`vert="horz"` 现在导入为显式 `horizontal` 而非被忽略，因为模型有这个值、显式 `horz` 能覆盖继承来的 `vert`，当作缺失会丢信息。

全仓 93 个测试文件 942 项通过（新增 run marks 11 项、换行 8 项、段落/body 21 项、model 1 项）；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。**明确延期**：`a:defRPr` 段落级默认继承（需 `defRPr → lstStyle → placeholder → master txStyles` 整条链）；只改 marks 不改字符的编辑仍不写回（`writeback.ts:926` 比较的是纯文本内容，导入先落地才有得比）；主题字体方案仍是硬编码 Aptos。

**阶段 6 文本格式写回切片已完成（设计 `1442150`，实现 `c555c4a`）**：补上上条明确预告的缺口 —— 只改格式、不改字符的编辑此前被静默忽略。**上一切片把导入接通后，「改文字时格式一起带出」已经自动可用**（探针里那条断言直接通过，无需改动），因此本切片只处理剩下的一种形状。

**探针六条、三通三失，失败的形状一致**：只改字体（`Georgia` → `Verdana`）、只改对齐（`ctr` → `r`）、清空 marks —— 三者输出都与源文件逐字相同。根因是 `writeback.ts` 用 `sourceTextContent` 与 `textBodyContent` 产出的**纯文本摘要**做判断（拼接所有 `a:t`、末尾补 `a:br` 个数个 `\n`），格式不在摘要里所以完全不可见。**这两个函数算法一致，因此此前的比较结果是正确的** —— 它准确回答了「文字内容变了吗」，只是那不是现在需要的问题。

**判据改为「两边都过同一个 `serializeTextBodyXml`，比较输出」**：字段顺序、属性顺序、省略规则全部由构造归一化，不需要写深比较也不需要 canonical JSON，而且语义恰好就是要问的问题 ——「按模型写出来的 XML，和源文件表达的意思，是否不同」。**否决了「拿序列化结果与源 `txBody` 原文直接比较」** —— 源文件的格式化、属性顺序、命名空间前缀几乎不可能与我们的序列化器逐字相同，那会让每次导出都重写每个 `txBody`、摧毁未建模内容。

**新增四个 `source*` 读取器镜像导入端的 `parse*`，是本文件的既有约定而非新发明** —— `writeback.ts` 已有四对同款镜像（`sourceColor` ↔ `parseColor`、`sourceBounds` ↔ `parseBounds`、`sourceRotation` ↔ `parseRotation`、`flipAttribute` ↔ `parseFlips`）。原因是**两个 XML 表示按设计不同**：导出端 `XmlElement` 带 `start`/`end` 字节偏移（范围写回必需）且直接有 `localName` 字段，导入端 `XmlNode` 两者都无。**否决了「让 pptx-export 真依赖 pptx-import 共享解析」**：那要把 `XmlNode` 加字段、解析函数泛型化到公共接口、并把 devDependency 升为运行时依赖，等于把「导出依赖导入」这条方向固化进架构。若镜像对继续增长，那是一个独立收敛切片，届时应连同已有四对一起做。

`sourceTextContent` 与 `textBodyContent` 唯一的调用点就是那一处，替换后即死代码，**直接删除而非留着「以防万一」**。`ScannedElement.sourceText: string` 改为 `sourceBody: TextBody`，扫描阶段一次解析、比较阶段复用。

**三条防回归断言最关键**：未编辑时字节完全相同、只改 bounds 时源 `rPr` 逐字保留、改文字时格式与对齐都在 —— 结构比较若有偏差会立刻在第一条炸。全仓 94 个测试文件 948 项通过；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。**剩余延期**：`a:defRPr` 继承链（其下游后果是靠 `defRPr` 表达的格式在模型中不可见，用户不碰该元素时两边都不含它、比较相等、不重写，故不丢；一碰则随整个 `txBody` 被替换）；`serializeAutofit` 的 `spAutoFit@lnSpcReduction` 错误；主题字体硬编码。

**阶段 6 表格单元格文本序列化收敛切片已完成（设计 `0f63850`，实现 `1fd73bc`）**：探针挖出一件此前两个切片都没暴露的事 —— **`table.ts` 里有第二份、且更弱的文本序列化器**，与 `standalone-xml.ts` 的那份并存。因此上两切片打通的格式往返在**表格单元格里是半通的**：导入端共用（`parseTextBody(cellNode)`）读全了段落属性，写出去时被丢掉一半。

**实测四处缺陷**：① **对齐值是错的** —— 输出 `algn="left"`/`algn="right"`（模型自己的词），而 OOXML 的 `ST_TextAlignType` 合法值是 `l`/`r`；**往返确认对齐读不回来**（重新导入后 `attrs` 是 `undefined`），`ctr` 那条恰好对；② 行距、段前后距、项目符号**全部静默丢弃**，连 `<a:pPr>` 都不生成；③ run 文本里的 `\n` 原样写进 `<a:t>` 而不转 `<a:br/>`；④ 首尾空格不加 `xml:space="preserve"`，符合规范的读者会裁掉。

**共享部分提到新文件 `text-xml.ts`，而非让 `table.ts` 反向导入 `standalone-xml.ts`** —— 当前模块方向是 `standalone-xml` → `table`（导入 `serializeTableXml`），反过来会**成环**。新方向 `standalone-xml` → `table` → `text-xml` 无环。`standalone-xml` 继续 re-export 三个函数，因此 `writeback.ts`、`theme-writeback.ts`、`master-layout-writeback.ts` 与 `index.ts` 的导入**一行未改**。`table.ts` 从 203 行降到 102 行。**否决了「把表格序列化搬进 `standalone-xml.ts`」** —— 那会让已有 380 行的文件再长 200 行，且表格与「无源包生成」是两个关注点。

`serializeTextBodyXml(body, prefix = 'p:')` 的标签前缀参数化，沿用 `serializeColorXml(color, prefix)` 的既有手法。**注意只有最外层 `txBody` 需要前缀**（形状 `p:`、表格 `a:`），内部的 `a:bodyPr`/`a:p`/`a:r` 在两种上下文里都是 `a:` —— 这与 `serializeColorXml` 里前缀贯穿所有子元素的情形不同，已写进注释。

**一处有意的输出变化**：表格单元格现在也带 `<a:lstStyle/>`（形状路径既有行为）。这是 `CT_TextBody` 的合法可选子元素，空元素表示「无覆盖」。**否决了「加 `includeListStyle` 开关让两条路径输出不同」** —— 为了让两边不一致而加参数，正是当初分叉的成因。`table.test.ts` 的逐字断言随之更新。

**顺带修掉 `spAutoFit` 的属性错误（两份序列化器里都有）**：此前把 `maxHeight` 写进 `lnSpcReduction`，而按 ECMA-376 **`spAutoFit` 无任何属性**、`lnSpcReduction` 属于 `normAutofit`（`A.normAutofit` 的文档明确列出它只有 `fontScale` 与 `lnSpcReduction` 两个属性）。现在写成裸 `<a:spAutoFit/>`。**`maxHeight` 保留在模型里** —— `layout.ts:483` 用它给 resize 高度设上限，是本项目自选的排版语义且有测试覆盖；它只是**不可持久化**，这一点由测试固定。**否决了「写成 `normAutofit@lnSpcReduction`」**（把「扩展形状高度」换成「压缩行距」，两者不是一回事）与**「从模型删掉 `maxHeight`」**（排版层在用，属独立切片）。

全仓 95 个测试文件 961 项通过（新增表格文本 13 项）；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。**剩余延期**：`a:defRPr` 继承链、主题字体硬编码 Aptos。

**阶段 6 占位符文本格式继承切片已完成（设计 `4af4e38`，实现 `bebbfc4`、`a23c694`）**：这是文本格式那一批切片的最后一块，且是它们暴露出来的。探针构造一个 layout，其 `title` 占位符带 `anchor="ctr"` + `algn="ctr"` + `sz="4400" b="1"` + Georgia，导入后 `defaults.title` 只有 `{bounds, preset, text}` —— **`ElementDefaults.body` 字段声明了但 `parseDefaults` 从不填它**，44pt 粗体 Georgia 居中一项都没进模型。

**值得记的是写回端与继承端早就准备好了**：`master-layout-writeback.ts` 的 `textReplacements` 优先用 `defaults.body`、回退到 `defaults.text`；模型的 `resolveInheritedElement` 用 `Object.assign` 按 master → layout → 元素顺序覆盖，`body` 作为整体字段天然参与。**只有解析端没填** —— 改动就是 `parseDefaults` 里加两行调 `parseTextBody`。

**`text` 与 `body` 并存而非二选一**：`text` 是既有公开字段，写回的 no-body 分支还在用它做比较，删掉会改变写回判断逻辑。代价是同一份内容存两遍，但 `body` 优先，语义无歧义。

**接上导入立刻暴露一个新问题，必须同一批修掉**：`defaults.body` 一旦真的有值，`textReplacements` 就会**无条件重写** placeholder 的 `txBody` —— 那条路径此前只在 no-body 分支比较，有 body 时直接替换。代价是丢掉源里我们不建模的内容：`a:lstStyle`、`a:defRPr`、未知子元素。判据改为与 `replaceSlideTables` 同款（两边都过 `serializeTextBodyXml` 再比较），并新增测试固定这三者在未编辑时逐字保留。

为此把 `sourceTextBody` 及其读取器从 `writeback.ts` 提到 `text-source.ts`、`sourceColor` 提到 `color-source.ts`。**注意 `master-layout-writeback.ts` 已有自己那份 `sourceColor`，返回值形状不同（带 `invalidKnownNode`），因此不动它** —— 只让两个新文件共用一份，没有强行三方统一。

**两条既有断言改用 `body` 而非 `text`**：`body` 现在优先于 `text`，与 `normalizeTextElement`（`element.body !== undefined` 时直接返回）的既有约定一致。

**明确划清与 `a:defRPr` 的边界**：本切片做的是 **`a:txBody` 级的占位符整体继承**，元素一旦有自己的 `body`（导入的幻灯片占位符总有）就完全接管，这点由测试固定。而 `a:defRPr` 是**段落级逐属性回退**，正确实现需要解析 `a:pPr/a:defRPr`、`a:lstStyle/a:lvl1pPr…lvl9pPr`、master 的 `p:txStyles/p:titleStyle|bodyStyle|otherStyle` 三层并按 level 逐属性合并，模型层还要新增「分级默认」结构、`resolveInheritedElement` 要从整体覆盖改为逐属性合并 —— 那是独立切片。

全仓 97 个测试文件 970 项通过（新增继承 7 项、占位符写回 2 项）；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。**剩余延期**：`a:defRPr` 分级默认链、主题字体硬编码 Aptos。

**阶段 6 主题字体方案切片已完成（设计 `4b61e29`，实现 `de59927`）**：探针给一个带完整 `fontScheme` 的主题（major Cambria/宋体，minor Calibri/等线）配一个用 `<a:latin typeface="+mj-lt"/>` 的标题，导入后是：

```
THEME: {"id":"theme_1","colors":{"dk1":{…}},"source":{…}}      ← 四个字体名一个都没进模型
RUN MARKS: {"fontFamily":"+mj-lt"}                              ← 引用被当字面字体名
```

`+mj-lt` 一路走到 `text-painting.ts` 的 `JSON.stringify(marks?.fontFamily ?? DEFAULT_FONT_FAMILY)`，画布拿到 `18px "+mj-lt"` —— 没有字体叫这个名字。**真实 PPTX 的占位符几乎全用主题引用**，所以这是常态而非边角。

**最值得记的实测发现：`measureText` 完全不看字体名**（只按字号与字符类别估宽）。因此本切片**只改字形、不改任何排版结果**，也因此不需要给 `layoutText` 加 `theme` 参数、不需要给就地编辑的 `TextBoxEditor` 铺主题 prop —— 唯一需要真实字体名的地方是画布 `context.font`，而它只从场景图取值。原以为要动 `packages/text` 的签名，读完 `measure.ts` 后这条路整个不必要。

**引用保留在模型里，渲染时才解析**：`text-xml.ts` 把 `marks.fontFamily` 原样写回 `<a:latin typeface>`，导入时展开成 Cambria 会让文本从「跟随主题」变成「钉死 Cambria」，且以后改主题字体不再联动。与 `{type:'scheme'}` 颜色同构。

**解析语义有意与颜色不同**：`resolveColor` 在主题缺槽位时返回 `undefined`（于是不画填充），而 `resolveThemeFontFamily` 只要认出六个 token 之一就一定给出具体字体名（模型值 → `DEFAULT_THEME_FONTS`）。理由是 `serializeThemeXml` 的兜底本来就是 Aptos，**让渲染与导出共用同一张默认表**，否则会出现「画布显示 Cambria、导出文件写 Aptos」的分叉。硬编码的 Aptos 字面量因此从 `standalone-xml.ts` 挪进模型的 `DEFAULT_THEME_FONTS`。

**`resolvedFontFamily` 只在解析改变了字体名时才挂**（镜像 `resolvedColor` 的兄弟字段约定，不动 `marks`）—— 不含主题引用的文本场景输出逐字不变，也不给每个 run 复制一份自己的字体名。marker（项目符号）一并解析，为此新增 `SceneTextLayoutMarker`；**但 marker 的颜色至今仍不解析**，那是先于本切片的独立缺口，本次没碰。

**绘制端加一行把「仍以 `+` 开头」的字体名当作没有字体**：OOXML 保留 `+` 前缀给主题引用，没有合法字体名以 `+` 开头。这一行兜住所有解析不出来的引用形式（例如源文件把 `+mj-ea` 写进 `<a:latin>`，而 EA 默认值为空），保证字面引用永不进 `context.font`；它只影响绘制，写回仍原样输出引用。

**两处行为变化**：①`<a:ea typeface=""/>`（Office 主题的常态，意为「无覆盖」）不进模型，与 `parseRunMarks` 已有的 `if (typeface)` 一致；②`parseTheme` 不再要求 `clrScheme` 存在 —— 只有字体没有颜色的主题现在也能导入并被 master 引用，此前会被整个丢弃。

**主题字体现在只能读不能改**（下一切片）：无 engine 命令、无面板 UI、`rewriteThemeXml` 仍只改 `clrScheme`。**下一切片必须先做写回再做命令**，否则重演 `a23c694` 那类问题。已核实 `fingerprintDocument` 走 `canonicalJson(整份文档)`，`fonts` 自动进指纹，所以字体一改就会走写回路径而非 byte-roundtrip —— 丢弃会发生在 `rewriteThemeXml` 内部，不是在导出选路上；写回要按最小差异做，源里的 `panose`/`pitchFamily`/`charset` 与 `<a:font script="…"/>` 都不建模，必须原样保留。

全仓 99 个测试文件 987 项通过（新增模型解析 3 项、导入 4 项、导出 2 项、场景 6 项、绘制 2 项）；递归类型检查、串行生产构建、12 包边界检查均通过。**剩余延期**：主题字体编辑（命令 + 面板 + 写回）、`fmtScheme` 样式矩阵、run 级 `<a:ea>`/`<a:cs>`、`<a:font script>` 回退表、`a:defRPr` 分级默认链。

> **2026-09-02 更正（适用于以下全部历史里程碑）**：第 97、101、104、106 行等处把「旋转元素的贴合选择框」列为待办 —— **该缺口不存在**，选择框本就随元素旋转（`SelectionOverlay.vue:88,100` 对 border 与 frame 施加 `transform: rotate(...)`，8 个手柄作为 frame 子元素随之旋转，`selection-overlay.test.ts:185` 一直在断言 `rotate(90deg)`）。误判源自 `2026-09-01-general-rotation-design.md` 决策 3 只读了纯几何的 `selection-overlay.ts`、未读 `.vue` 组件，此后被逐条里程碑沿用。**历史条目保持原文不改**（它们记录的是当时的认知），以本注为准。真实缺口只剩多选联合 bounds 按轴对齐计算，已含在「多选整体旋转」内。
>
> 同批更正的另一类问题：设计文档中三处「PowerPoint 的行为是……」属**无法核实的断言**（本机无阅读器），已在对应 spec 中删除或标注为假设。此类关于其他应用行为的具体描述今后一律标注不确定。

**阶段 7 group 旋转级联切片已完成**：`GroupElement` 此前连 `rotation` 字段都没有，本切片补上字段并让旋转在 scene 拍平时级联进全部后代。

**核心决策是「级联发生在 scene 构建期，模型只存自身旋转」**。理由不是改动小，而是 engine 的 `resize` 用轴对齐 `mapBounds` 缩放后代（`engine/src/index.ts:910-933`）——若把祖先旋转烘焙进模型 bounds，缩放会产生斜切。把旋转留在渲染期，等价于「在 group 未旋转的本地空间里缩放、再整体旋转」，正是 PowerPoint 的语义。

`@ppt4ai/geometry` 新增 `cascadeRotation(bounds, rotation, ancestors)`，按 `{支点, 角度}` 链**从内向外折叠**——内层 group 先转自己的内容，外层才搬动整体，因此支点必须是各祖先**未受更外层影响的原始中心**。把已旋转的矩形绕外部一点再转仍是同尺寸矩形，所以宽高精确不变、只有中心移动，级联不引入形变；这一点由测试直接固定。

**一处实现顺序上的坑由测试逼出来**：最初写成「先 `createNode`、再改 node 的 bounds」，但 text 与 table 的 layout 持有由 bounds 推导的**绝对坐标**（行盒、单元格 rect），事后移动 bounds 会把 layout 落在原地。改为在 `createNode` **之前**级联元素，并加了两条断言钉住 `layout.bounds` 必须与 `node.bounds` 一致。

`SceneGroup` 新增生效旋转（自身 + 全部祖先），命中测试两处硬编码的 `rotation: undefined` 随之删除，进入 group 前的边界判断也改为旋转感知。`PptEditor.vue` 的 `selectedRotatableNode` 因为要同时容纳 group（**不是 `SceneNode`**，只存在于 `scene.groups`）与四种节点，改为返回归一化的 `{ id, bounds, rotation, kind? }`，调用方从读 `node.transform?.rotation` 改为读 `node.rotation`。engine 与 playground 的 kind 守卫从白名单反转为「只拒 image」（image 走 `transform.rotation` 嵌套落点，仍由 `setImageRotation` 负责）；`validateDocument` 的整数校验同样改为「除 image 外都查」。

**改写了两条既有断言**：`PptEditor.test.ts` 里「选中 group 时不显示旋转把手」现在改为显示；`engine.test.ts` 里「group 不可旋转」那行删除，但该用例其余的原子性与 undo/redo 断言保留。两处都是有意翻转契约，不是回归。

聚焦 geometry/render/engine 7 个文件 124 项、全仓 75 个测试文件 753 项通过；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。设计提交为 `17b29b3`，实现提交为 `7a440f1`。

**两个已知限制，均由测试固定而非隐藏**：① **group 旋转不进 PPTX** —— 导入器（`importer.ts:474`）只匹配 `sp`/`graphicFrame`/`pic` 并递归穿过 `p:grpSp`，因此**导入的分组会被拍平成散元素，`GroupElement` 只由编辑器 group 命令产生**；写回侧（`writeback.ts:461`）同样不扫 group，且元素按位置索引一对一映射，引入 group 会移位并触发 `element prefix mismatch`。② **ungroup 会丢失 group 自身的旋转** —— 后代只存自身旋转、不含祖先贡献，解组后那份旋转无处可去（烘焙需同时重算 bounds，属独立切片）。多选整体旋转、旋转元素的贴合选择框、group 翻转继续延期。

**阶段 7 旋转数学收敛切片已完成**：动手前先做的纯重构。原以为有三份重复的 OOXML 六万分之一度换算，grep 后实际是**五份**：两份完整的点旋转实现（`slide-canvas.ts` 吃 EMU 空间、`table-editor-overlay.ts` 吃屏幕空间）、`PptEditor.vue` 里第三份用于「旋转下的 resize 补偿」、`image-painting.ts` 内联的裸常量，以及 `image-transform.ts` 里**反方向**的 `10800000 / Math.PI`。后两者正是 `@ppt4ai/geometry` 要成对导出 `rotationRadians` 与 `rotationUnitsFromRadians` 的原因。五处调用点全部改为委托，`boundsCentre` / `rotatePointAround` / `containsRotatedPoint` 一并归位。**唯一没有统一的是矩形类型** —— EMU 侧用 `{w,h}`、overlay 侧用 `{width,height}`，强行统一需要在每个调用点加适配层，因此 overlay 保留三行本地包装自算中心。`packages/editor` 新增 `@ppt4ai/geometry` 依赖（headless、无 Vue，边界检查通过），需 `pnpm install` 后下游测试才能解析。行为零变化，既有旋转测试守护搬迁，新增 geometry 测试钉住调用方依赖的不变量（中心不动、可加性、取负即逆、整圈为恒等）。实现提交为 `a3a38a7`。

**阶段 7 table 旋转编辑入口切片已完成**：补上上条里程碑明确预告的缺口 —— 数据与呈现早已就位，缺的只是产生旋转的入口与一处会错位的命中测试。改动共四处 kind 判断加一段数学：`EditorEngine.setElementRotation` 与 `apps/playground` 的 `rotateSelectedElement` 各放宽一个 kind 守卫；`PptEditor.vue` 的 `selectedRotatableNode` 纳入 table，旋转手势、预览、overlay 角度、工具栏左右旋转按钮与 `rotate-element` 事件链本就与 kind 无关，**除该判断式外未改一行**（`rotate-element` 经 App.vue → presentation-host → asset-host 三层转发，读码确认全程不按 kind 分派）。真正需要设计的是单元格命中：cell overlay 由 `layoutRectToScreen` 生成轴对齐矩形，若只旋转绘制层、不旋转命中层，点击会落到视觉之外的单元格。解法是 overlay 容器加 CSS `rotate(rotation / 60000 deg)`（单元格按容器定位，随容器一起转；`transform-origin` 默认 `50% 50%`，与下述支点一致），`tableCellAtPoint` 先把点绕 bounds 中心反向旋转回本地坐标再做原有矩形判定。**未复用 `slide-canvas.ts` 的 `containsRotated`** —— 那个函数是模块私有且吃 EMU 空间的 `{w,h}` 矩形，overlay 用的是屏幕空间 `{width,height}`，因此写了同构的本地 `unrotatePoint` 并加注释指明对应关系，而非导出改造既有函数（**后续收敛切片 `a3a38a7` 已把两者统一到 `@ppt4ai/geometry`**）。`rotation` 进入 `TableEditorOverlayModel`，未旋转时走原轴对齐快路径。engine 里那条「table 不可旋转」的断言被改为用 group 作为仍不可旋转的 kind，**该用例的意义被保留而非删除**（**该断言随后又被 group 旋转切片移除，见上文**）。全仓 72 个测试文件 709 项通过（engine 72、editor overlay 8、playground asset-host 26）；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过。**过程更正一则**：首个实现提交的信息声称打通了「编辑入口」，但当时 `selectedRotatableNode` 仍排除 table，界面上并无任何旋转把手 —— 该遗漏由后续自查发现并单独补提，勿以第一条提交信息为准。group 复合旋转、多选整体旋转、shape/text 翻转、旋转元素的贴合选择框继续延期；**行列拖拽调整把手本不存在**（行列增删是结构化命令，无拖拽把手），故不存在「旋转后把手方向错乱」问题。设计提交为 `ff0dcff`，实现提交为 `fbde8ac` 与 `859bf25`。


**阶段 7 table 旋转切片已完成**：补上此前唯一缺字段的元素类型 —— `TableElement` 新增可选 `rotation`，`validateDocument` 把整数校验从 `shape | text` 放宽到含 `table`，单位仍是 Office 六万分之一度、不在模型层转弧度。五层各自复用既有管道而非新开 table 专用路径：导入侧 `parseTable` 直接调 `parseRotation(frame)`（该函数用 `findDescendants(shape, 'xfrm')`，天然匹配 graphicFrame 上的 `p:xfrm`，无需改动）；写回侧在 table 分支追加 `rotationReplacements(...)`，仍只替换 `p:xfrm` 的 `rot` 属性与 `tbl` 节点，保守写回边界未被放宽；standalone 序列化给 `p:xfrm` 加 `attrs([['rot', table.rotation]])`，与 shape 路径同款、undefined 自动省略；`createTableNode` 用与另两个 builder 逐字相同的 `if (element.rotation) node.transform = ...` 透传。渲染侧 `paintTableNode` 用共享 `withRotation` 包住填充、边框与文本三轮绘制，**pivot 取映射后的 `mappedRect(node.bounds, mapping)`** —— 单元格在映射空间作画，用模型坐标当支点会偏移；命中测试删掉 `node.kind === 'table' ? undefined` 特例（顶层与组内两处同一行，一并去除），旋转表格现在与 shape/text 走同一条反向旋转判定。缩略图无需改动：`thumbnail-worker.ts` 委托同一个 `paintTableNode`，自动继承。全仓 72 个测试文件 703 项通过（editor 261、新增 12 项）；递归类型检查、串行生产构建、12 包边界检查与 `git diff --check` 均通过（`git diff --check` 输出的 LF/CRLF 是 Windows 行尾提示，非错误）。**仓库确无 `check:elementplus` 脚本**，沿用上条里程碑的更正。group 复合旋转、多选整体旋转、shape/text 翻转、旋转元素的贴合选择框继续延期；table 旋转**没有 UI 入口**（`selectedRotatableNode` 未纳入 table，本切片只打通数据与呈现，导入的旋转表格能正确显示与命中，但界面上无法新产生）。实现提交为 `46b41a6`。

**阶段 7 shape/text 旋转编辑切片已完成**：这是旋转链路的最后一段 —— 渲染、命中与写回此前已就位，缺的只是产生旋转的编辑入口。`@ppt4ai/engine` 新增 `setElementRotation`，按 kind 分派落点：image 仍走 `transform.rotation` 并复用 `normalizeImageTransform`，shape/text 直接写裸 `rotation` 字段；`rotation === 0` 时删除字段而非存 `0`，与 image 既有约定一致。**未做 modulo 归一化，也没有角度范围校验** —— 读码确认 image 路径同样只校验整数性，本切片沿用该先例而非发明新约束。`setImageRotation` 原样保留。`@ppt4ai/editor` 把 `selectedImageNode` 泛化为 `selectedRotatableNode`（单选且 kind 属 shape/text/image），旋转手势、预览、overlay 角度与工具栏左右旋转按钮一并生效，非 image 发新的 `rotate-element` event；翻转按钮仍是 image 专属，`SelectionOverlay.vue` 未改动，只改了决定是否传 `show-rotation-handle` 的判断式。`apps/playground` 三层宿主（asset-host → presentation-host → App.vue）接通新命令。全仓 72 个测试文件 691 项、engine 70 项通过；递归类型检查、串行生产构建、12 包边界检查和 `git diff --check` 均通过。**注意仓库无 `check:elementplus` 脚本，只有 `check:boundaries`** —— 此前里程碑文案里的「Element Plus 扫描」在 `package.json` 中无对应实现，勿据此以为有该门禁。table 旋转（模型缺字段）、shape/text 翻转、多选与 group 复合旋转、旋转元素的贴合选择框按设计文档明确延期。设计提交为 `51ac8bc`，实现提交为 `4d27bcf`。

**阶段 7 通用旋转渲染与命中测试切片已完成**：`@ppt4ai/render` 的 `createShapeNode` / `createTextNode` 现与既有 image 节点一致透传 `transform`，`rotation` / `flipH` / `flipV` 不再在建图阶段被丢弃，Office 六万分之一度单位原样保留、不在此层转弧度。`@ppt4ai/editor` 新增共享的 `rotation-transform.ts`，围绕元素中心 `translate → rotate → translate back` 就地旋转，因此 shape 的 `createPath` 与 text 的绝对映射坐标绘制代码无需改写；`image-painting.ts` 沿用自身既有约定，本切片不动它。命中测试改为把点反向旋转回元素本地坐标后再做矩形判定，顶层与组内两条路径一致，flip 不影响覆盖区域，未旋转节点仍走原轴对齐快路径。table 旋转与 group 复合旋转按设计文档明确延期，旋转句柄与旋转吸附亦未包含。旋转聚焦测试 5 个文件 36 项、全仓 72 个源码测试文件 680 项均通过；递归类型检查、串行生产构建、12 包边界检查、Element Plus 扫描和 diff 检查通过。特征测试提交为 `43e948a`，设计提交为 `d1c7627`。

**阶段 7 多选缩放、比例锁定与吸附切片已完成**：`@ppt4ai/engine` 增加原子 `resizeSelection` 命令，以联合 bounds 映射多选根对象及嵌套 group 后代，保留选择顺序并只产生一个 patch history；`@ppt4ai/editor` 为联合选区恢复八个 resize 控制角，支持四角 Shift 比例锁定、边/角确定性网格与顶层对象吸附及临时参考线，单选 legacy resize API 保持兼容。`apps/playground` 接入统一 snap 配置和 `resizeSelected` 宿主命令，状态文案补齐且不再产生 i18n 缺失警告。编辑器聚焦测试 3 个文件 52 项、engine/Playground 聚焦测试 3 个文件 82 项、编辑器 30 个源码测试文件 207 项、全仓 57 个源码测试文件 493 项均通过；递归类型检查、生产构建、12 包边界检查、Element Plus 扫描和 diff 检查通过。设计提交为 `55cf142`，计划提交为 `ee03a61`，实现提交为 `9acf730`。

**阶段 7 Group/Ungroup 工具栏交互切片已完成**：`@ppt4ai/editor` 增加受控顶层多选 `selectedElementIds` 与 `selection-change`，同时保留 `selectedElementId` 和 legacy `select`；Shift/Ctrl/Meta 点击可切换成员，普通空白点击清空，多个对象显示联合边框且不显示 resize 控制角。普通点击已选成员会在抬起时折叠为单选，而拖拽已选成员会保持整组选区并一次移动全部对象。原生 UnoCSS Group/Ungroup 按钮具备本地化、可见 focus、disabled 语义，并在进入 group 后禁用；Playground 宿主复用 engine 原子 `group`/`ungroup` 命令，组合后选中新 group，取消组合后恢复原成员选区。编辑器聚焦测试 2 个文件 27 项、Playground 聚焦测试 2 个文件 24 项、编辑器 29 个源码测试文件 189 项、全仓 56 个源码测试文件 470 项均通过；递归类型检查、生产构建、12 包边界检查、Element Plus 扫描和 diff 检查通过。实现提交为 `a9b92b4`。

**阶段 7 group 缩略图语义切片已完成**：缩略图 Worker 继续只按 `@ppt4ai/render` 的扁平 `SceneGraph.nodes` 顺序绘制叶节点，嵌套 `SceneGraph.groups` 只作为 JSON-safe、可 `structuredClone` 的交互元数据存在；group ID 不进入 `drawnNodeIds`、`skippedNodeIds` 或 issue，空 group 元数据场景会成功生成空缩略图。分组内 shape/text/table/image 仍复用既有 painter、图片资源桥和逐叶节点失败隔离，后续叶节点不会被前一个失败节点阻断。该切片仅增加协议克隆、嵌套顺序、空场景、资源与失败隔离的特征测试，没有增加专用 group painter、递归 group 遍历或新协议字段。编辑器 29 个源码测试文件共 180 项、全仓 56 个源码测试文件共 456 项测试、递归类型检查、构建、12 包边界检查、Element Plus 扫描和 diff 检查均通过。

**阶段 7 表格缩略图切片已完成**：`@ppt4ai/editor` 的 Worker/OffscreenCanvas 缩略图现按三段顺序绘制 `SceneTableNode` 的解析单元格填充、四边边框和预计算文字布局；边框支持 solid/dash/dot/none、1 pt 默认宽度和一像素可见下限，并按源单元格及 left/right/top/bottom 顺序确定性处理共享边。表格文字复用既有布局级 painter，不在 Worker 内测量、换行或重排。shape/image/text/table 保持原始节点顺序和逐节点失败隔离；空表格计为成功，表格不发起资产资源请求，缩略图协议保持不变。51 个测试文件共 404 项测试、12 包边界检查、递归类型检查和构建均通过；chart/group 缩略图、共享边冲突解析、渐变/阴影等高级表格效果和浏览器字体嵌入继续延期。

**阶段 7 本地图片上传切片已完成**：`apps/playground` 现可从本地选择 PNG、JPEG、GIF、BMP 和 WebP，经过浏览器 File reader 复制字节并由宿主生成确定性的 `asset_upload_N` 资产 ID；上传成功后可插入新图片或替换当前图片，资产元数据、原始字节、选区和 undo/redo 均保持一致。上传事务先写入宿主管理的 `AssetAdapter`，成功后才 dispatch engine；校验失败、读取失败和事务失败均映射为本地化状态，失败不会推进序列或写入 adapter。adapter 二进制仍由宿主管理，undo/redo 只恢复文档元数据与引用，不自动删除 orphan bytes。拖拽、剪贴板、多文件上传、持久化资产库和孤儿资源自动清理继续延期；group 缩略图与主编辑器画布宿主集成仍是下一步。52 个测试文件共 416 项测试、边界检查、类型检查、构建和 Element Plus 扫描均通过。

**阶段 7 主编辑器画布接入切片已完成**：`@ppt4ai/editor` 现以统一高 DPI Canvas 顺序绘制 shape、text、table、image SceneGraph 节点，并对每个节点隔离绘制失败；`SlideCanvas` 按 EMU 到 CSS px 的 viewport 变换执行逆序矩形命中测试，`PptEditor` 以受控 props 组合画布、选中边框和八个 resize 控制点。`apps/playground` 已从 engine 当前文档生成真实 SceneGraph，点击 shape/text/table/image 会回写 engine selection，选中不会增加 undo history；缩略图、素材库、本地图片上传、拖拽/缩放提交和文本/表格画布内编辑保持可用。当前仍明确延期：group/chart、多页导航、剪贴板和持久化资产库。

**阶段 7 画布变换提交切片已完成**：`SlideCanvas` 现通过 Pointer Capture 追踪单指拖拽，以 EMU 发出稳定的起点、预览增量和结束增量；正常 `pointerup` 才提交移动，`pointercancel` 只释放 capture 并清理手势。`SelectionOverlay` 与 `PptEditor` 现提供八方向 resize 预览，固定起始 bounds、补偿句柄按下偏移，并在结束时将 CSS px 几何转换回 EMU；无活动手势或取消手势不会提交。Playground 宿主将每次移动或 resize 结束映射为一条 engine command，因此每个手势只增加一次 undo history。group/chart、多页导航、剪贴板和持久化资产库继续延期。

**阶段 7 画布文本编辑切片已完成**：双击文本元素会进入画布内编辑，`PptEditor` 复用现有 `TextBoxEditor` 的 IME、光标、选区和自动换行能力；编辑态保留选中边框与八个缩放控制点。`Ctrl+Enter` 或失焦正常提交，`Escape` 取消；一次编辑最多产生一个 engine undo 事务，空值与未变化内容不会制造多余历史记录。`EditorEngine` 通过 `setTextBody` 更新文本，Playground 以标准化 `TextBody` 接入真实文档状态。

**阶段 7 group 画布交互切片已完成**：`@ppt4ai/render` 为嵌套 group 保留 JSON-safe 的 bounds、childIds、ancestorIds 与 paintOrder 元数据，同时继续按扁平叶子顺序绘制；`@ppt4ai/editor` 将 group 作为不可穿透的顶层选择目标，复用选中边框和八个缩放控制点；`@ppt4ai/engine` 支持外层 group、嵌套 group 与全部后代叶子的递归移动，以及按 X/Y 独立比例映射的 group 缩放，并在单个 commit 中保持 undo/redo 原子性。Playground 已将 demo shape/text 接入真实 `group_demo`，点击选择、拖拽移动和缩放均走现有受控事件流。边界检查、Element Plus 扫描、56 个测试文件共 448 项测试、类型检查、构建和 diff 检查均通过。组内文本编辑、旋转、吸附、保持比例与 group toolbar 继续延期。

**阶段 7 group 进入与组内选择切片已完成**：`PptEditor` 维护仅由字符串组成的本地 group path；顶层点击继续选择整个 group，双击 group 逐层进入，进入后命中仅返回当前 group 的直接子 group 或直接叶子节点。嵌套 group 可继续双击进入，`Escape` 逐层退出并回选父 group；点击当前 group 边界外会清空上下文并重新执行顶层命中，scene 更新时失效路径会截断到仍有效的前缀。`SlideCanvas` 使用独立 `enter-group` 事件区分 group 进入与普通文本激活，文档模型、SceneGraph 绘制协议和 engine API 均未改变。56 个源码测试文件共 453 项测试、递归类型检查、构建、12 包边界检查、Element Plus 扫描和 diff 检查均通过。旋转、吸附、保持比例和 group toolbar 继续延期。

设计阶段已完成。架构设计总纲已确认并落盘：[`docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md`](docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md)，含 29 条已确认决策、5 节技术设计、13 个阶段的路线图。

阶段 0 Task 1-9 已完成。隐藏 contenteditable 输入桥、Canvas 组词绘制、Chromium CDP 自动化，以及 Windows 原生中文输入法候选框的 Chrome/Edge 人工验收均已通过；Task 6 判定 GO。Task 7 完成 12 个包骨架和边界检查器；Task 8 完成 Vue 3 + UnoCSS 编辑器外壳与语义 i18n；Task 9 完成冻结安装、全量测试、类型检查和构建验证。详见 [阶段 0 全量验证](docs/verification/2026-08-22-stage-0-verification.md)。

**阶段 1 已完成**：`@ppt4ai/model`、`@ppt4ai/geometry` 和 `@ppt4ai/render` 已建立 JSON-safe 文档模型、预设几何路径与确定性 SceneGraph；模型、几何和渲染快照测试均已建立。边界检查、37 个单测、类型检查和构建全部通过。

**阶段 2 已完成**：`@ppt4ai/pptx-import` 已支持最小真实 OOXML 导入、ZIP stored/deflate 解压、namespace XML 解析、slide/layout/master 关系链、EMU 几何、结构化颜色、文本和 XML source 快照；SceneGraph 已接入三级稀疏继承。共享 layout/master 会按目标路径复用，导入结果保持 JSON-safe 与 `structuredClone`-safe。边界检查、46 个单测、类型检查和构建全部通过。

**阶段 3 已完成**：`@ppt4ai/engine` 已提供 JSON-safe、headless 的 command-driven 编辑会话，支持单选/多选/additive selection、EMU bounds 移动与 resize、网格/元素边缘与中心吸附、patch-based undo/redo、z-order、扁平 group/ungroup；`@ppt4ai/model` 与 `@ppt4ai/render` 已支持 group 校验、循环防护和递归 SceneGraph 展开。60 个单测、边界检查、类型检查和构建全部通过。下一目标为阶段 4 文本 layout、ProseMirror 与 IME。

**阶段 4 第一切片已完成**：`@ppt4ai/model` 增加 JSON-safe 文本 body/run/paragraph/marks/autofit 契约与路径化校验；`@ppt4ai/text` 提供 legacy text 规范化、固定 EMU 字符测量、中英文/CJK 自动换行、对齐/缩进/间距/垂直对齐，以及 `none` / `shrink` / `resize` Autofit；`@ppt4ai/render` 将确定性行盒布局接入 SceneGraph，并保留 legacy `text` 兼容字段。全量边界检查、84 个单测、类型检查和构建均通过。本切片保持 headless，不引入 Element Plus、DOM、Canvas 或系统字体依赖；ProseMirror、IME 编辑事务和浏览器编辑桥留待下一切片。

**阶段 4 第二切片已完成**：`@ppt4ai/text` 增加不依赖 DOM 的 ProseMirror schema、`TextBody` 双向转换、选区事务、段落拆分、反向删除和 IME composition 生命周期；composition 结束只提交一次，并抑制紧随其后的重复 `text-input`。`@ppt4ai/editor` 增加 `createTextEditorController`，将现有隐藏 `contenteditable` IME bridge 接入单一编辑状态，支持快照、focus、dispatch 和幂等 destroy。边界检查、96 个单测、类型检查和构建均通过。本切片仍不包含视觉光标/选区映射、格式化命令、项目符号、竖排、表格或 PPTX 导出。

**阶段 4 第三切片已完成**：`@ppt4ai/editor` 增加 headless 选择框几何、八个角点/边中点控制点、最小尺寸约束和 `SelectionOverlay.vue`；激活态渲染边框与控制点，resize 结果可直接进入现有 `EditorEngine` `resize` 命令并保留历史记录。根级 Vitest 已接入 Vue SFC 转换。

**阶段 4 第四切片已完成**：`@ppt4ai/text` 增加基于确定性行盒的 ProseMirror UTF-16 位置到 caret/selection 几何映射与 layout hit-test，覆盖软换行、显式段落和空段落；`@ppt4ai/editor` 增加统一 EMU/layout 到屏幕 px 的 viewport transform、文本交互几何和 UnoCSS `TextEditorOverlay.vue`；`TextEditorController` 增加 `syncCaret`，在 focus 时重复最近光标矩形并同步隐藏 IME bridge。边界检查、122 个单测、类型检查、构建和 diff 检查全部通过。本切片仍不包含格式化命令、项目符号、竖排、表格或 PPTX 导出。

**阶段 4 第五切片已完成**：`@ppt4ai/editor` 增加可复用 `TextBoxEditor.vue`，组合文本框激活生命周期、pointer 单击命中、正反向拖选、现有 caret/selection overlay 和隐藏 IME bridge；`TextEditorController` 增加 selection transaction 与 JSON-safe 快照订阅。composition provisional 文本只进入临时预览 layout，并用 UnoCSS 下划线呈现，正文保持不变；composition end 只外发一次提交后的 `TextBody`。边界检查、126 个单测、类型检查、构建和 diff 检查全部通过。本切片未引入 Element Plus 或新的运行时依赖。

**阶段 4 第六切片已完成**：`@ppt4ai/text` 增加 `setTextMarks`、`toggleTextMark`、`setTextAlignment` 和 `getTextFormattingState`，支持选区格式化、collapsed selection stored marks、混合状态查询、反向选区和多段落对齐；`@ppt4ai/editor` 扩展 controller 与 `TextBoxEditor` 的格式状态事件，并新增 Vue + UnoCSS `TextFormattingToolbar.vue` 受控工具栏及中英文 i18n。全量边界检查、测试、类型检查、构建和 diff 检查通过；未引入 Element Plus 或新的运行时依赖。

**阶段 4 项目符号与编号切片已完成**：`@ppt4ai/model` 增加可校验的字符项目符号、Arabic/大小写字母编号契约；`@ppt4ai/text` 将项目符号映射到 ProseMirror 段落属性，提供选区感知的设置/清除命令，并在确定性 layout 中输出首行 marker、连续编号、层级独立编号、自动换行和 autofit 几何；`@ppt4ai/pptx-import` 支持常见 `buChar`/`buAutoNum` 结构化导入，marker 不写入 run 文本。全量边界检查、147 个单测、类型检查、构建和 diff 检查通过；未引入 Element Plus 或新的运行时依赖。下一步为竖排文本；表格与 PPTX 导出仍未完成。

**阶段 4 基础竖排文本切片已完成**：`@ppt4ai/model` 增加 `TextBodyProperties.vertical` 契约与校验；`@ppt4ai/text` 支持从右向左的列布局、列内从上到下的字符排列、CJK/全角 upright 与 Latin/数字/标点 rotated 元数据、自动换列、对齐、项目符号/编号、autofit，以及竖排 caret、selection 和 hit-test；`@ppt4ai/pptx-import` 映射 `vert270`、`vert`、`wordArtVert` 并保留 legacy 文本字段。该阶段文本切片保持 headless，不引入 Element Plus 或新的运行时依赖。

**阶段 7 表格核心与单元格文本布局已完成**：`@ppt4ai/model` 增加 JSON-safe `TableElement`、行列尺寸、单元格跨度、填充、四边框和重叠校验；`@ppt4ai/layout` 增加基于 EMU 累计偏移的确定性合并单元格布局；`@ppt4ai/pptx-import` 导入 `a:tbl` 的网格、行高、文本、`gridSpan`、`rowSpan`、`hMerge`、`vMerge`、单元格填充和 `lnL/R/T/B`，坏表格只跳过自身并保留同页其他元素；`@ppt4ai/render` 暴露 `SceneTableNode`，并为普通、合并和空单元格生成现有确定性 `TextLayout`。本切片共 172 个全量测试通过，单元格文本聚焦测试 40 项通过，边界检查、类型检查、构建和 `structuredClone` 验证均通过。提交为 `0f87118`、`9fd3bf8`、`ed224f6`、`53d903c`、`c1ba898`。

**阶段 7 表格样式语义切片已完成**：`@ppt4ai/model` 增加 JSON-safe 表格样式定义、引用、校验和纯单元格样式 resolver，按 whole-table、横纵 band、首尾行列和显式单元格属性确定性合并；`@ppt4ai/pptx-import` 导入 `ppt/tableStyles.xml` 的基础填充/四边框以及 `tblPr` 样式 flags，并对损坏或不支持的片段稳定降级；`@ppt4ai/render` 为每个 `SceneTableLayoutCell` 暴露 clone-safe `resolvedStyle`，同时保留源单元格 `fill` 和 `borders`。完整主题颜色/PowerPoint 样式继承、表格编辑 UI 和 PPTX 表格导出 XML 仍待后续切片。提交为 `e1c97a6`、`1357769`、`d994c14`。

**阶段 7 表格结构编辑切片已完成**：`@ppt4ai/engine` 增加 headless `insertTableRow`、`deleteTableRow`、`insertTableColumn`、`deleteTableColumn` commands；行列插入/删除会保持合法网格、调整合并单元格跨度、生成空单元格、同步表格 bounds，并迁移或裁剪 `tableCellSelection`。每个操作均先经过模型校验，再以单一 patch 写入 history，支持原子 undo/redo；22 个 engine 聚焦测试、边界检查、全量测试、类型检查和构建均通过。设计、计划、测试契约和实现提交为 `91d5c3c`、`b4d450c`、`45cb21e`、`5e96e15`。合并/拆分、表格结构 UI、表格文本编辑、完整主题继承和 PPTX 表格导出 XML 仍待后续切片。

**阶段 7 表格编辑引擎切片已完成**：`@ppt4ai/engine` 增加独立、JSON-safe 的表格单元格选区，支持合并单元格源坐标归一化、矩形范围选区、当前单元格文本替换，以及选区填充和四边框独立编辑；所有文档修改复用 patch-based undo/redo，一条命令原子提交，非法样式不会部分写入历史。`@ppt4ai/model` 补齐显式单元格填充校验。本切片保持 headless，不引入 Element Plus 或新运行时依赖；UnoCSS 表格 UI、行列增删、合并拆分、完整主题继承和 PPTX 表格导出仍待后续。

**阶段 7 表格文本编辑切片已完成**：`@ppt4ai/editor` 复用现有 `TextBoxEditor`、确定性文本 layout 和 Chromium IME bridge，在普通及合并源单元格完整 bounds 内提供自动换行、段落输入、caret、selection 和 composition 预览；表格会话隐藏文本框 resize 边框与八个控制角，但普通文本框默认行为保持不变。编辑期间只更新 clone-safe 本地草稿，`Ctrl+Enter`/失焦一次提交，`Escape` 取消，composition 中关闭请求延迟至最终 body 发布；独立 headless controller 在提交前重新选择目标源单元格，再复用 `setTableCellText` 生成至多一个 undo history。表格 overlay 支持 Enter 与双击进入编辑，Space 仍只选择；未引入 Element Plus 或新运行时依赖。完整主题颜色/样式继承和 PPTX 表格导出 XML 仍待后续。
**阶段 7 表格 XML 序列化切片已完成**：`@ppt4ai/pptx-export` 暴露纯函数 `serializeTableXml`，确定性输出 `a:tbl` payload，包含网格列宽、行高、`gridSpan`/`rowSpan`、纵向 `vMerge` continuation、文本 body/run marks、结构化颜色及有序 transforms、表格/单元格填充、四边框和 table-style flags；`system` color 保留 fallback RGB，`none` border 输出 `a:noFill`，源模型保持 clone-safe。4 个聚焦测试、包级类型检查、构建和 `git diff --check` 均通过，提交为 `77d66b2`。

**阶段 7 表格源包写回切片已完成**：`@ppt4ai/pptx-export` 增加 `exportPptx(document, source)`，从原始 ZIP 保留条目顺序、未知 XML、关系链和二进制资源，按 importer 的 slide/element 顺序只替换匹配的 `a:tbl` XML；缺失关系、源 part 和元素数量不一致时稳定报错，重复导出保持确定性且不修改文档。ZIP 读写、导入修改回写、邻近 XML/二进制保留和 mismatch 聚焦测试、包级类型检查和构建均通过，提交为 `b011188`、`2306479`。全新文档 package 生成和图片处理仍待后续切片。

**阶段 7 图片资产基础切片已完成**：`@ppt4ai/model` 增加 clone-safe `ImageElement`、图片 MIME/元数据和异步 `AssetAdapter` 契约，文档 JSON 只保存稳定资产引用；`@ppt4ai/pptx-import` 按 slide 元素顺序导入普通 `p:pic`，解析关系与 PNG/JPEG/GIF/BMP/WebP 头部，生成稳定 asset ID，并将原始字节按唯一资源写入可选 adapter，坏图片只跳过自身；`@ppt4ai/render` 增加带 bounds、asset ID 和元数据快照的 `SceneImageNode`，不读取或解码二进制。提交为 `fddddd4`、`847e814`、`b483e07`。图片解码与实际绘制、裁剪/蒙版/旋转/效果、插入/替换、导出写回和 UI 资产管理明确留待后续切片。

**阶段 7 浏览器图片渲染切片已完成**：`@ppt4ai/editor` 增加按 SceneGraph 顺序绘制图片的 Canvas renderer，支持 EMU 到 CSS/backing pixel 转换、缩放和高 DPI；按 `assetId` 缓存成功或失败的解码结果，去重并发加载，并对缺失资源、解码失败和绘制失败做单节点局部降级。默认 Chromium 解码器使用 `createImageBitmap`，`ImageCanvas.vue` 负责 canvas 生命周期、过期渲染抑制和卸载清理，诊断结果保持 `structuredClone`-safe。实现仅依赖 Vue 与 UnoCSS，未引入 Element Plus 或新的运行时依赖；裁剪、蒙版、旋转、效果、缩略图/worker、图片插入/替换、PPTX 写回和资产管理 UI 明确留待后续切片。提交为 `a935af8`、`a7369e1`、`401e20f`、`b95f9d2`；74 个聚焦测试、36 个测试文件共 270 个全量测试、边界检查、类型检查和构建全部通过。

**阶段 7 图片外观与 Worker 缩略图切片已完成**：共享图片 painter 支持源裁剪、rect/roundRect/ellipse/triangle 蒙版、OOXML 旋转、水平/垂直翻转、`alphaModFix` 和 `grayscl`；`@ppt4ai/editor` 增加 clone-safe Worker 协议、`OffscreenCanvas` 图片缩略图 runtime、主线程 `AssetAdapter` 资源桥、资源去重缓存、latest-request-wins 取消和过期 bitmap 关闭，并由 UnoCSS-only `ThumbnailCanvas.vue` 管理生命周期。真实 Chromium smoke 已验证 Worker 脚本、资源桥、非空位图和连续场景更新；库构建使用相对资源基址，确保 Worker chunk 从消费包的 `dist/assets` 加载。shape、text、table、chart 和 group 缩略图绘制仍明确延期，图片插入/替换、PPTX 写回和资产管理 UI 尚未实现。

**阶段 7 图片插入/替换与资产生命周期切片已完成**：`@ppt4ai/model` 提供 PNG/JPEG/GIF/BMP/WebP 共享位图元数据解析，供 PPTX 导入与编辑上传复用；`@ppt4ai/engine` 增加同步、headless、clone-safe 的 `insertImage` 和 `replaceImageAsset` 命令，保持图片外观字段、引用计数清理、原子失败和 undo/redo；`@ppt4ai/editor` 增加异步图片资产控制器，校验并复制字节、完成 `AssetAdapter.put()` 后才 dispatch，并在写入后命令失败时报告 orphan asset ID。adapter 二进制继续由宿主管理且不会自动删除，undo/redo 只同步恢复文档元数据与引用。PPTX media part、relationship 与 `p:pic` 写回是下一切片，其后实现仅依赖 UnoCSS 的资产管理 UI；shape/text/table/chart/group 缩略图绘制继续延期。

**阶段 7 PPTX 图片写回切片已完成**：`@ppt4ai/pptx-export` 在保留源 ZIP 条目、无关 XML 和既有媒体的前提下，将 importer 可识别的 `p:pic` 纳入有序源元素映射；未修改图片复用原 relationship/media 且不读取 adapter，替换图片通过 `AssetAdapter.get()` 写入确定性 media part、追加 relationship 并只替换原 `r:embed`，尾部插入图片生成完整 `p:pic`。相同新 asset ID 只读取和写入一份媒体，缺失 adapter/字节/元数据、MIME 不匹配、关系损坏、删除/重排和非图片尾部元素均原子拒绝。下一切片为仅依赖 Vue、Vue-I18n 与 UnoCSS 的资产管理 UI；shape/text/table/chart/group 缩略图绘制继续延期。

**阶段 7 资产管理 UI 切片已完成**：`@ppt4ai/editor` 增加可复用 UnoCSS `AssetLibrary.vue` 及纯 `createAssetLibraryModel`，按文件名和 asset ID 稳定排序，展示图片格式、尺寸与本地化 fallback，支持选中、键盘选择、双击插入以及插入/替换 intent；缩略图复用现有 Worker 管线，单个缩略图失败只影响自身并保留其他资产。组件不调用 engine、不写入 `AssetAdapter`，上传、adapter 写入和宿主 wiring 留在集成层；shape/text/table/chart/group 缩略图绘制继续延期。Task 2 聚焦测试 4 项、编辑器类型检查通过，提交为 `c301068`、`e06e295`。

> ⚠️ 阶段 0 的 IME 预研是**硬性前置**，不是可选的探索。它是全项目技术风险最高的一块；若走不通，纯 Canvas 架构需要重新考虑 —— 那种事必须在写几万行代码之前发现。

## 二、阶段进度

| 阶段 | 目标 | 状态 | 验收标准 |
|---|---|---|---|
| — | 需求梳理 + 架构设计总纲 | ✅ 完成 | 29 条决策确认，spec 落盘 |
| **0** | 工程骨架 + **IME 技术预研** | ✅ 完成 | IME 人工 GO；12 包边界、29/29 单测、5/5 E2E、类型检查和构建均通过；冻结安装验证通过 |
| 1 | model + geometry + render | ✅ 完成 | JSON-safe 模型、预设几何路径、SceneGraph 快照测试；边界检查、37 个单测、类型检查和构建均通过 |
| 2 | pptx 导入（至可渲染） | ✅ 完成 | 最小真实 OOXML 导入、共享关系复用、SceneGraph 三级母版继承；边界检查、46 个单测、类型检查和构建均通过 |
| 3 | engine 编辑交互 | ✅ 完成 | 选择/变换/吸附/历史/z序/组合，全部可在 node 里断言 |
| 4 | 文本（layout + ProseMirror + IME） | ✅ 完成 | 确定性 layout、ProseMirror、IME 编辑事务、格式化、项目符号/编号和基础竖排文本均已完成 |
| 5 | editor UI 外壳 + i18n | ⬜ 未开始 | Vue 3 自研面板体系 + UnoCSS；zh-CN / en-US 双语 |
| 6 | pptx 导出（含无损回写） | 🚧 进行中 | 源 PPTX 表格、图片、页面结构、版式关系、媒体内容类型及 shape/text body、bounds、填充、描边、预设几何与旋转可确定性写回；无源 package、依赖 part 克隆/生命周期和无修改源字节往返已完成，剩余高级属性与兼容阅读器验证仍待实现 |
| 7 | 表格 + 图片处理 | 🚧 进行中 | 表格能力、图片处理、shape/image/text/table 缩略图、group 扁平叶节点缩略图语义、源 package 写回、资产管理 UI、本地图片上传、主编辑器画布、拖拽/缩放提交、文本/表格画布内编辑和 group 进入选择已完成；chart、多页导航及更高层工作流待后续 |
| 8 | 图表（lyCharts fork） | ⬜ 未开始 | 8 种图表可编辑数据 |
| 9 | 母版/版式编辑器 | ⬜ 未开始 | 改母版所有引用页自动更新；换主题换版式生效 |
| 10 | 动画 | ⬜ 未开始 | 三类动画 + 触发时序 + 切页；动画能导出到 pptx |
| 11 | player 放映/演讲者视图 | ⬜ 未开始 | 独立引入不背编辑器体积；备注、画笔、计时 |
| 12 | 打磨 | ⬜ 未开始 | 性能达标、EMF/OLE 三级降级、SmartArt 矢量渲染 |

图例：✅ 完成 · 🚧 进行中 · ⬜ 未开始 · ⚠️ 受阻

## 三、本阶段任务清单

实施计划：[docs/superpowers/plans/2026-08-23-stage-4-vertical-text.md](docs/superpowers/plans/2026-08-23-stage-4-vertical-text.md)

- [x] 建立 JSON-safe TextBody / paragraph / run / marks / Autofit 契约与路径化校验
- [x] 规范化 legacy `text`，实现固定 EMU 字符测量和 JSON-safe 文本布局输出
- [x] 实现 CJK 逐字、英文按词及超长词按字符自动换行
- [x] 实现对齐、缩进、段前/段后间距、行距和垂直对齐
- [x] 实现 `none` / `shrink` / `resize` Autofit，并接入 SceneGraph 行盒
- [x] 完成 `pnpm check:boundaries`、`pnpm test`、`pnpm typecheck` 和 `pnpm build`

- [x] 实现字符格式化事务、stored marks、toggle 与输入校验
- [x] 实现混合/统一格式状态查询和多段落对齐
- [x] 接入 controller、TextBoxEditor 格式事件和受控 UnoCSS 工具栏
- [x] 完成 `pnpm check:boundaries`、`pnpm test`、`pnpm typecheck`、`pnpm build` 和 `git diff --check`

- [x] 建立字符项目符号与 Arabic/字母编号模型契约和校验
- [x] 实现 ProseMirror 项目符号映射、选区命令与反向选择保持
- [x] 实现首行 marker、连续编号、层级序列、自动换行和 autofit 几何
- [x] 导入 PPTX `buChar`、常见 `buAutoNum` 与 `startAt`
- [x] 完成边界检查、147 个单测、类型检查、构建和 diff 检查

- [x] 建立 `TextBodyProperties.vertical` 模型契约与校验
- [x] 实现从右向左竖排列布局、自动换列、对齐、项目符号/编号和 autofit
- [x] 实现竖排 caret、selection 几何映射与按列 hit-test
- [x] 导入 PPTX `vert270`、`vert` 和 `wordArtVert` 写法
- [x] 完成边界检查、23 个测试文件共 163 个测试、类型检查、构建和 diff 检查

后续工作：继续阶段 7 的完整表格能力，再实现 PPTX 导出；保持模型、排版、渲染和导入包的 headless 边界。

实施计划：[`docs/superpowers/plans/2026-08-23-stage-7-table-core.md`](docs/superpowers/plans/2026-08-23-stage-7-table-core.md)

- [x] 建立 `TableElement`、`TableRow`、`TableCell`、填充和四边框模型契约与路径化校验
- [x] 实现 EMU 累计列/行偏移、`rowSpan`/`colSpan` 和确定性边框布局
- [x] 导入 DrawingML `a:tbl` 网格、行高、文本、填充、边框和合并标记归一化
- [x] 将表格布局接入 `SceneTableNode`，保留节点顺序、继承属性和 clone safety
- [x] 将普通、合并和空单元格 `TextBody` 接入现有确定性 `TextLayout`
- [x] 完成边界检查、172 个全量测试、单元格文本聚焦测试、类型检查、构建和 `git diff --check`
- [x] 建立表格样式定义、引用、校验和确定性单元格样式解析
- [x] 导入基础 `tableStyles.xml` 区域样式与 `tblPr` flags，并接入 SceneGraph `resolvedStyle`
- [x] 建立独立表格单元格选区、合并坐标归一化和矩形范围枚举
- [x] 实现选中单元格文本、填充和四边框编辑，并接入原子 undo/redo
- [x] 实现 UnoCSS 表格编辑 UI（表格边框、源单元格命中、单击/拖选、合并单元格坐标归一化和键盘激活）
- [x] 通过 headless controller 接入表格选区与 engine 命令，保持 Vue overlay 与 engine 解耦
- [x] 实现 UnoCSS 填充/边框工具栏，并通过 controller 接入 engine 格式命令
- [x] 实现行列增删、完整矩形合并与合并单元格拆分，并接入选区归一化、内容保留、校验和原子 undo/redo
- [x] 实现合并感知的表格文本编辑、本地 IME 草稿、自动换行和单次提交/取消语义
- [x] 实现完整主题颜色/样式继承（模型解析、PPTX 导入与 SceneGraph resolved paints/text）
- [x] 实现表格 `a:tbl` XML payload 序列化（`77d66b2`）
- [x] 将表格 XML 写回完整 PPTX ZIP/package，并接入关系链与 slide replacement

实施计划：[`docs/superpowers/plans/2026-08-23-stage-7-image-assets.md`](docs/superpowers/plans/2026-08-23-stage-7-image-assets.md)

图片编辑实施计划：[`docs/superpowers/plans/2026-08-23-stage-7-image-editing-assets.md`](docs/superpowers/plans/2026-08-23-stage-7-image-editing-assets.md)

- [x] 建立 clone-safe 图片元素、资产元数据和异步 `AssetAdapter` 契约
- [x] 导入普通 `p:pic`、稳定 asset ID、原始位图字节和 PNG/JPEG/GIF/BMP/WebP 尺寸
- [x] 保持 slide 元素顺序、唯一资源单次写入，并对损坏或缺失图片局部降级
- [x] 将图片引用与元数据接入 clone-safe `SceneImageNode`
- [x] 实现浏览器图片解码、缓存、失败隔离与高 DPI Canvas 实际绘制
- [x] 增加 Vue `ImageCanvas` 宿主、过期渲染抑制和卸载资源清理
- [x] 实现图片裁剪、蒙版、旋转、效果：浏览器 Canvas 支持源裁剪、rect/roundRect/ellipse/triangle 蒙版、OOXML 旋转与水平/垂直翻转、alphaModFix 和 grayscl
- [x] 实现 Worker + OffscreenCanvas 图片缩略图、主线程资源桥、缓存、取消、过期结果抑制和 Chromium smoke 验证
- [x] 实现 Worker 基础形状缩略图：rect / roundRect / ellipse / triangle 路径、解析颜色与 alpha、原始节点顺序和逐节点失败隔离
- [x] 实现 Worker 文字缩略图：预计算水平 run 与 marker、基础 upright/rotated 竖排、字体 marks、解析颜色与 alpha、单下划线、空节点成功和逐节点失败隔离；文字不请求资产且协议不变
- [x] 实现 Worker 表格缩略图：解析单元格填充、四边 solid/dash/dot/none 边框、预计算文字、三段式顺序、空表成功和逐节点失败隔离；表格不请求资产且协议不变
- [x] 实现图片插入/替换、共享位图校验、文档资产引用清理和原子 undo/redo
- [x] 实现 adapter 先写后 dispatch、失败隔离与 orphan asset ID 报告；adapter 二进制保持宿主所有
- [x] 接入 PPTX media part、relationship 与 `p:pic` 导出写回，支持未修改复用、替换与尾部插入
- [x] 实现仅依赖 UnoCSS 的资产管理 UI；chart 缩略图绘制继续延期
- [x] 接入 Playground 本地图片上传：确定性 asset ID、插入/替换、失败隔离和本地化状态；拖拽、剪贴板、多文件、持久化和 orphan 清理由宿主后续处理
- [x] 验证 group 缩略图沿用扁平叶节点绘制：嵌套顺序、叶节点结果 ID、空场景、图片资源、失败隔离和 clone safety 均保持既有协议

图片变换实施计划：[`docs/superpowers/plans/2026-08-29-stage-7-image-transform-editing.md`](docs/superpowers/plans/2026-08-29-stage-7-image-transform-editing.md)

- [x] 实现图片专用旋转/翻转 engine 命令、原子校验、no-op 和 undo/redo
- [x] 实现旋转手柄、OOXML 角度计算、Shift 15° 吸附和受控旋转预览
- [x] 实现 Alt/Alt+Shift 中心缩放、居中吸附和单 guide 比例锁定
- [x] 接入 Playground 图片变换按钮、宿主 dispatch、本地化状态和集成测试
- [x] 完成 editor 227 项、engine 59 项、Playground 34 项及全仓 519 项测试、边界检查、类型检查、构建和 Element Plus 扫描

多页导航实施计划：[`docs/superpowers/plans/2026-08-30-stage-7-multipage-navigation.md`](docs/superpowers/plans/2026-08-30-stage-7-multipage-navigation.md)

- [x] 让 Playground asset host 支持注入页面文档与共享 adapter
- [x] 新增 presentation host，隔离各页 Engine、selection、history 和当前页命令
- [x] 接入基于现有 ThumbnailCanvas 的 UnoCSS 原生缩略图列表与当前页同步
- [x] 完成 Playground 41 项、全仓 526 项测试、边界检查、类型检查、构建和 Element Plus 扫描

页面生命周期实施计划（短设计）：

- [x] 新增空白页面并激活，保持新页面 Engine history 为空
- [x] 深拷贝当前页面并生成独立 page/document ID
- [x] 删除当前页面并在最后一页时阻止删除
- [x] 上移/下移页面且保持页面状态、selection 和 history 不变
- [x] 接入 UnoCSS 原生操作栏、本地化反馈和 6 项新增集成测试
- [x] 完成 Playground 47 项、全仓 532 项测试、边界检查、类型检查、构建和 Element Plus 扫描

页面结构历史实施（2026-08-31）：

- [x] 为单页 asset host 暴露编辑 undo/redo，并保持 selection、文档状态和引擎历史
- [x] 为新增、复制、删除和排序页面建立 presentation 级结构历史
- [x] 结构撤销/重做保留页面 host 实例、页面 ID、selection 和页面内编辑历史
- [x] 当前页存在编辑历史时优先撤销页面编辑；新的结构操作清空 presentation redo 栈
- [x] 完成 Playground 14 项 presentation-host 测试、全仓 537 项测试、边界检查、类型检查和构建

跨页元素剪贴板核心实施（2026-08-31）：

- [x] 为 Engine 增加通用 `insertElements` 原子命令，统一元素子树、资产元数据、校验、selection 和 undo/redo
- [x] 在 presentation host 保存 clone-safe 选区快照，复制 group 时递归收集子树并去除重复嵌套选区
- [x] 粘贴到任意页面时重映射元素 ID，复用兼容图片资产元数据，并在资产冲突时复制字节到新资产 ID
- [x] 将一次跨页粘贴收敛为一条页面 Engine 历史；无选区不污染剪贴板或历史
- [x] 修正注入页面宿主的图片插入/上传 slide ID 路由，完成 78 项 Engine/Playground 聚焦测试和全量类型检查

跨页剪贴板 UI 实施（2026-08-31）：

- [x] 接入 UnoCSS 原生撤销、重做、复制、粘贴操作栏与按钮可用态
- [x] 接入 `Ctrl/Cmd+C`、`Ctrl/Cmd+V`、`Ctrl/Cmd+Z`、`Ctrl/Cmd+Y` 和 `Ctrl/Cmd+Shift+Z`
- [x] 全局快捷键避让输入框、textarea、select 和 contenteditable，保持 IME 编辑目标优先
- [x] 补齐中英文操作状态、本页/页面结构历史深度和剪贴板对象数展示
- [x] 完成 Playground 18 项 presentation-host、16 项 App 测试、全仓 544 项测试、边界检查、类型检查和串行构建

- [x] 建立 browser-compatible ZIP stored/deflate 与纯 XML tokenizer 基础层
- [x] 扩展 slide/layout/master/source JSON-safe 模型及稀疏继承 resolver
- [x] 导入 presentation、relationships、slide、layout、master 与基础 shape/text
- [x] 接入 SceneGraph 并验证 slide > layout > master 继承和共享关系复用
- [x] 完成边界检查、46 个单测、类型检查和构建验证

- [x] 建立 JSON-safe 文档模型与结构校验
- [x] 实现 rect / roundRect / ellipse / triangle 预设几何路径
- [x] 建立确定性 JSON SceneGraph 转换
- [x] 增加模型、几何与 SceneGraph 快照测试
- [x] 完成边界检查、全量测试、类型检查和构建验证

阶段 0 历史任务：

- [x] 建立最小 IME 风险实验外壳
- [x] 定义浏览器无关 IME 状态契约
- [x] 实现隐藏 contenteditable 输入桥
- [x] 绘制组词文本与 Canvas 光标
- [x] 用 Chromium CDP 自动化验证 composition
- [x] 完成 Windows 中文输入法 go/no-go 验收（Chrome/Edge 全部通过）
- [x] GO 后扩展完整 pnpm workspace（Task 7 已完成）
- [x] 增加最小 Vue 外壳与语义化 i18n（Task 8 已完成）
- [x] 在干净 worktree 完成阶段 0 全量验证（Task 9 已完成）

## 四、决策台账

当前 **29 条决策**见 spec [§8 决策台账](docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md#8-决策台账)，含每条的理由与已知代价。**此处只记录实施期间新增或推翻的决策**，避免两处重复导致漂移。

| 日期 | 决策 | 理由 | 状态 |
|---|---|---|---|
| 2026-08-22 | UI 层移除 Element Plus，只使用 UnoCSS | 减少依赖与组件框架约束，编辑器专用控件由项目自行实现 | ✅ 已同步到架构总纲和阶段计划 |

## 五、已知问题与风险

完整风险表见 spec [§9](docs/superpowers/specs/2026-08-22-ppt4ai-architecture-design.md#9-已知风险)。最高两条摘录：

| 风险 | 等级 | 缓解 |
|---|---|---|
| canvas 自绘光标 + 中文 IME | 🔴 最高 | 阶段 0 强制预研；走不通则纯 Canvas 架构需重新考虑 |
| 图表 pptx 覆盖面（决策 18 的已知代价） | 🔴 高 | lyCharts 需自追 percentStacked / 次数值轴 / `c:numFmt` / 完整 `dLbls` / 对数轴 / 组合图 等，预估 2-4 个月 |
| EMF/WMF 图片在编辑器中不可见 | 🟡 中 | `importer.ts:551`：非位图媒体使 `parseBitmapMetadata` 返回 `undefined`，`p:pic` 不进模型且无警告，因此画布上看不到、选不中。**但 pptx 文件本身不丢**：未编辑时走 byte-roundtrip 原样返回；编辑其他元素时范围写回把 `p:pic` 与媒体字节作为未建模内容原样保留（`emf-roundtrip.test.ts` 固定此行为）。要真正可编辑需合规解码方案（`emf-to-png` 禁装）；中间步骤可考虑导入为占位元素并上报 issue，使不可见状态对用户可知 |

**2026-09-01 已核实为「已完成、非待办」**：opaque part 保留与媒体 content-type 组合此前列在待办里，实际已实现 —— `dependency-graph.ts:39-55` 区分共享部件与可克隆依赖（`notesSlide` 单数为页私有、随页克隆），`writeback.test.ts:641-647` 覆盖 notesSlide 克隆及其嵌套 `customXml` sidecar 与 content-type override，`image-writeback.ts:12-21` 覆盖 `ImageMimeType` 全部 5 种并处理扩展名冲突。勿重做。

**待办的外部任务**：去 lyCharts 仓库修 4 个 bug（单例串扰、grid 单调缩小、33 处样式静默失效、arcTo 圆角柱报错）—— 独立任务，不占本项目工期（决策 19）。

**许可证纪律**（详见 spec §10）：

- PPTist 是 **AGPL-3.0**，网络传染 —— 绝不读、绝不移植、绝不派生其 `src/`
- `emf-to-png` 声明 MIT 实则打包 GPL wasm —— **禁装**

## 六、下次开工从这里继续

1. 在可用阅读器环境下用 PowerPoint、LibreOffice 和兼容阅读器打开无源及编辑后输出，记录修复项
2. EMF/WMF 仍不进模型（编辑器中不可见不可选），但导入已通过 `onIssue` 上报，文件层面不丢。**占位元素方案已否决**（会破坏导出索引对齐，见里程碑与 `emf-roundtrip.test.ts`）。剩余选项：先解决合规解码（`emf-to-png` 声明 MIT 实为 GPL wasm，禁装）。**注意**：「在 playground 呈现 issue」暂时无处落地 —— playground 从不调用 `importPptx`（种子文档由 `asset-host.ts` 代码构造），要呈现导入 issue 需先做文件导入 UI，那是独立切片
3. 跨页主题同步 —— **不建议改 engine**。`packages/editor` 里单个 engine 持整份文档，主题编辑本就是文档级正确的；页级作用域是 playground `pageDocument()` 逐页 `structuredClone` 的产物，属 demo 外壳自身的模型选择。要修就修 playground 分页模型（让各页共享一份文档），不要为迁就脚手架给 engine 加「文档级命令不记入单页历史」的特例
4. **主题字体已能读取与解析，剩下的是编辑**：`fontScheme` 进了 `Theme.fonts`，`+mj-lt`/`+mn-lt` 由 `resolveThemeFontFamily` 在场景图里解析成 `resolvedFontFamily`，`serializeThemeXml` 改读模型（默认值集中在 `DEFAULT_THEME_FONTS`）。**下一步顺序是「先写回、再命令、最后面板」** —— `rewriteThemeXml` 目前只改 `clrScheme`，而 `fonts` 已进 `fingerprintDocument` 的指纹，所以一旦有了 `setThemeFont` 命令而写回还没跟上，编辑会在 `rewriteThemeXml` 内部被静默丢弃（同 `a23c694`）。写回按最小差异做：`panose`/`pitchFamily`/`charset` 与 `<a:font script="…"/>` 都不建模，必须原样保留。**`fmtScheme` 仍不在模型中**（要连带 `<p:style>` 的 `idx` 引用与 `phClr` 代入才有意义）。主题增删切换与 master/layout 绑定编辑仍未评估
5. **文本格式的剩余缺口**：run marks（七项）、段落属性（八项）、body 属性（insets/anchor/wrap/vert/三种 autofit）现已完整往返，模型、排版、绘制、导出四层本就支持；**只改格式不改字符的编辑也已能写回**（判据改为两边同过 `serializeTextBodyXml` 比较）；**表格单元格与形状现在走同一条序列化路径**（此前 `table.ts` 有第二份更弱的实现，`algn` 输出模型自己的词导致对齐读不回来，行距/项目符号/`a:br`/`xml:space` 全丢）；**layout/master 占位符的文本格式也已进入 `ElementDefaults.body` 并能继承**（该字段此前声明了但从不填）。**剩余只有一项**：`a:defRPr` **段落级逐属性回退**未做 —— 需要解析 `a:pPr/a:defRPr`、`a:lstStyle/a:lvl1pPr…lvl9pPr`、master 的 `p:txStyles/p:titleStyle|bodyStyle|otherStyle` 三层并按 level 合并，模型层要新增「分级默认」结构、`resolveInheritedElement` 要从整体覆盖改为逐属性合并，是独立切片。**注意它与已完成的占位符继承不同**：后者是 `txBody` 级整体继承，元素有自己的 `body` 就完全接管。下游后果是靠 `defRPr` 表达的格式在模型中不可见，但**不丢** —— 未编辑时两边序列化都不含它、比较相等、不重写（`placeholder-body-writeback.test.ts` 固定了这点）；一旦编辑该元素，`defRPr` 会随整个 `txBody` 被替换。**另注 `TextAutofit` 的 `maxHeight` 是「排版层可用但不可持久化」的字段** —— `spAutoFit` 按 ECMA-376 无任何属性，导出写裸元素、导入读不到该值，`layout.ts:483` 仍在用它给 resize 高度设上限
6. **旋转与翻转的剩余缺口**：shape/text/image/table/group 五种 kind 的模型字段、渲染、命中、编辑手势与工具栏按钮均已打通，**旋转与翻转两条链路都已完整**；group 的旋转与翻转都在 scene 拍平时级联进全部后代（含嵌套），命中测试消费 `SceneGroup.rotation`/`flipH`/`flipV`；`p:grpSp` 已能导入、写回（含 `off`/`ext`、`chOff`/`chExt` 与翻转）并映射子坐标空间；`move`/`resize` 在带 `childSpace` 的组处停止改写后代，避免变换施加两次；**ungroup 把组的旋转、子空间与翻转烘焙进整棵子树**，解组不再造成视觉跳变（角度落在叶子上，因为 group 只能绕自身中心旋转 —— 见里程碑追加四）。**三个曾被列为待办的条目已核实是伪缺口或已完成**：①「`childSpace` 不随 resize 同步」是伪缺口（固定 `ext:chExt` 比例等于固定缩放系数，会让组框变大而内容不动）；②「旋转元素的选择框是轴对齐外框」也是伪缺口 ——`SelectionOverlay.vue:88,100` 早已对 border 与 frame 施加 `transform: rotate(...)`，手柄随 frame 旋转，`selection-overlay.test.ts:185` 一直在断言；该误判源自设计文档只读了纯几何的 `selection-overlay.ts`、未读 `.vue`，已在 4 份 spec 中更正；③多选整体旋转与 shape/text/table/group 翻转**均已完成**（见里程碑追加五、追加六）；④多选整体翻转**也已完成**（追加七），且实现思路与追加六自己写的预告**相反** —— 逐元素翻转、不动 bounds，而非绕联合中心镜像重算 bounds，理由见追加七。**剩余只有一项**：「翻转形状但不镜像其中文本」这条语义取自微软文档、**未经阅读器实测**，是有阅读器后第一批该核对的项
7. 保持 adapter 二进制由宿主管理，undo/redo 只恢复文档元数据与引用
8. **新写涉及 barrel 的测试时，用静态 `import * as x from './index'`，不要在测试体内 `await import()`** —— 后者把整个 barrel 的 transform 压进 5s 单测超时，是此前偶发失败的根因（已修，见文首注）
9. chart、animation、浏览器字体嵌入及高级形状/文字效果继续延期（group 的选择、移动、缩放、进入、组合、旋转、PPTX 往返与解组烘焙已完成，剩余项见第 6 条）

- 2026-08-22：Task 6 Windows 中文 IME 人工门禁已确认 GO；Chrome/Edge、80%/100%/125% 缩放及可用的 150% 显示缩放场景全部通过，允许进入 Task 7 工作区骨架。
